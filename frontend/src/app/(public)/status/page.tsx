import { StubPage } from "../_components/stub-page";

export const metadata = { title: "Status · AI Career OS" };

export default function StatusPage() {
  return (
    <StubPage
      title="System Status"
      description="All core services — API, agents, database, and payments — are operating normally. A real-time status page with history and incident reports is coming soon."
    />
  );
}
