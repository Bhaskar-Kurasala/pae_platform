import { StubPage } from "../_components/stub-page";

export const metadata = { title: "Changelog · AI Career OS" };

export default function ChangelogPage() {
  return (
    <StubPage
      title="Changelog"
      description="Shipped updates, roadmap milestones, and deprecations. The detailed changelog moves here once we hit public launch."
    />
  );
}
