# ── Environment ──
.env
.env.local
.env.production
*.env

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-test/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
coverage.xml
.coverage

# ── Node ──
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# ── Docker ──
docker-compose.override.yml

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ── Claude Code: track shared config (skills/agents/commands/hooks/rules/
#    settings.json); ignore only personal + session artifacts ──
.claude/settings.local.json
.claude/worktrees/
CLAUDE.local.md
docs/claude-code-prompts/

# Generated mirror of .claude/skills/ for other agent runtimes — do not track
# (regenerated from .claude/skills/, which is the source of truth).
.agents/

# ── Build ──
dist/
build/
*.egg
