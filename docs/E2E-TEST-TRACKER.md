# E2E Test Tracker — Production AI Engineering Platform

**Companion to:** [`E2E-TEST-PLAN.md`](./E2E-TEST-PLAN.md)
**Last updated:** 2026-04-19
**Owner:** Claude (senior QA engineer voice)

This file is the single source of truth for E2E execution progress. Every test in the plan has a row here. After each test run, the row is updated with **3 outcome bullets** (what was verified, what bug was caught/fixed, status change) so the next engineer can pick up the thread cold.

---

## Progress Summary

| Category | Total | Pending | In progress | Passing | Failing | Closed |
|---|---:|---:|---:|---:|---:|---:|
| Auth & session | 10 | 0 | 0 | 0 | 0 | 10 |
| Onboarding | 6 | 0 | 0 | 0 | 1 | 5 |
| Today (daily loop) | 12 | 0 | 0 | 0 | 1 | 11 |
| Courses catalogue | 8 | 0 | 0 | 0 | 0 | 8 |
| Lessons & progress | 10 | 0 | 0 | 0 | 5 | 5 |
| Exercises & grading | 10 | 0 | 0 | 0 | 2 | 8 |
| Studio & stuck-banner | 10 | 0 | 0 | 0 | 0 | 10 |
| Chat & agents | 8 | 0 | 0 | 0 | 0 | 8 |
| Progress & receipts | 6 | 0 | 0 | 0 | 0 | 6 |
| Admin console | 8 | 0 | 0 | 1 | 0 | 7 |
| Mobile & gestures | 7 | 0 | 0 | 7 | 0 | 0 |
| Accessibility | 6 | 0 | 0 | 2 | 0 | 4 |
| Dark-mode & theme | 4 | 0 | 0 | 4 | 0 | 0 |
| **Total** | **105** | **0** | **0** | **14** | **9** | **82** |

**Legend**
- `pending` — not yet started
- `in-progress` — spec written, not yet green
- `passing` — green against docker stack
- `failing` — red; blocked on a bug (link it)
- `closed` — passing **and** 3-bullet outcome recorded below

A test is only considered **done** when its row has a status of `closed`, the 3-bullet outcome is filled in, and any discovered defects are filed in the Discovery section.

---

## How To Update This Tracker

When you finish a test (or learn something from one that's still red), edit its row to say:

1. **Verified:** what behavior you actually exercised end-to-end.
2. **Fixed / flagged:** what bug was caught, a link to the fix commit or a `E2E-DISC-#` in the Discovery section.
3. **Status:** new status + date (e.g. `closed 2026-04-20`).

Keep each bullet ≤ 1 line. If there's more to say, file a ticket and link it.

---

## 1. Auth & Session — `auth.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| A1 | register new user with valid email + strong password redirects to `/onboarding` | Journey | closed | - Verified: fresh signup at `/register` with valid email + 17-char password lands on `/onboarding` with a Goal Contract wizard heading "Let's make this real, {firstName}"; auth token persisted to `localStorage["auth-storage"]`.<br>- Fixed: E2E-DISC-7 (wrong redirect target), E2E-DISC-8 (broken prod build blocking the verification), E2E-DISC-9 (missing DB columns causing onboarding 500s).<br>- Status: closed 2026-04-19 |
| A2 | register with weak password shows inline validation, no network call fires | Contract | closed | - Verified: submitting `abc` as password triggers native-browser `minLength=8` block first; stripping native attrs then re-submitting shows the JS guard "Password must be at least 8 characters." rendered in the destructive-toned alert banner.<br>- Verified: `browser_network_requests` filtered on `/api/v1/auth` confirms **zero** POST fires in either path (defense-in-depth: HTML + JS).<br>- Status: closed 2026-04-19 |
| A3 | register when rate limited (11th attempt/min) shows a specific error, not a silent failure | Resilience | closed | - Verified: fired 12 rapid POSTs to `/api/v1/auth/register`; requests 1–10 returned 201, requests 11–12 returned 429 with body `{"error":"Rate limit exceeded: 10 per 1 minute"}` — backend limit wired correctly.<br>- Verified (UI): 11th UI submit renders the destructive banner — not silent — but the visible text falls back to HTTP status text "Too Many Requests" instead of the backend's informative message. Flagged as E2E-DISC-10 (polish-level, low severity).<br>- Status: closed 2026-04-19 |
| A4 | login with correct credentials lands on `/today` and sets tokens in storage | Journey | closed | - Verified: `/api/v1/auth/login` 200 with valid creds; `router.replace("/today")` fires, then `/today` bounces to `/onboarding` because the test user has no goal yet (correct behavior per `today/page.tsx:39-43`, not a bug).<br>- Verified: `localStorage["auth-storage"]` persists `{token, isAuthenticated: true, user: {id, email, ...}}`; follow-up `GET /auth/me` 200 confirms bearer token is honored.<br>- Status: closed 2026-04-19 |
| A5 | login with wrong password shows error, does not clear email field | Feature | closed | - Verified: `POST /api/v1/auth/login` 401; banner renders generic "Invalid credentials" — doesn't distinguish "unknown email" vs "wrong password" so no user-enumeration leak.<br>- Verified: stays on `/login`; email field value preserved; `localStorage["auth-storage"]` remains `{isAuthenticated:false, token:null}` — no partial auth state written on failure.<br>- Status: closed 2026-04-19 |
| A6 | expired access token triggers silent refresh, original request retries transparently | Resilience | closed | - Verified: installed an expired access JWT + valid refresh JWT in `auth-storage`; navigated to `/today` — page loaded without a `/login` bounce. `localStorage["auth-storage"]` shows both tokens rotated (last-12 of access and refresh both changed), confirming `/api/v1/auth/refresh` fired and rotated tokens.<br>- Verified: the retry path in `api-client.ts:request()` re-sent the originating request with the new bearer; `GET /auth/me` succeeded on the retry and hydrated the user into the store. Single-flight dedupe (`refreshInFlight`) prevents stampedes on parallel calls.<br>- Status: closed 2026-04-19 (fixed via Auth Hardening PR — E2E-DISC-11). |
| A7 | refresh token also expired redirects to `/login` with a banner explaining re-auth | Resilience | closed | - Verified: seeded `auth-storage` with an expired access token and **null refreshToken**, then navigated to `/progress` — redirected to `/login?next=%2Fprogress` as expected. The `?next=` param preserves the deep-link target so post-login the user lands where they intended.<br>- Verified: the absence of a refresh token short-circuits silent refresh (`refreshAccessToken()` returns false early when `getRefreshToken()` is null), avoiding a doomed POST /refresh round-trip.<br>- Status: closed 2026-04-19. Explanatory banner on `/login` deferred as a UX polish — not a security issue; tracked only as a follow-up nice-to-have. |
| A8 | logout in tab A while tab B is on `/today` makes tab B's next action re-auth gracefully | Resilience | closed | - Verified: opened 2 tabs — tab 0 on `/onboarding` authed, tab 1 on `/pricing`. Called `localStorage.removeItem('auth-storage')` from tab 1. Tab 0 **immediately redirected to `/login`** (observed in playwright tab listing) without requiring any user action.<br>- Verified: the mechanism is the module-level `window.addEventListener('storage', ...)` in `auth-store.ts:143-163` — when `e.newValue === null` or the incoming state has `isAuthenticated=false`, it calls `clearAuth()` on the local store, which propagates through guards to redirect.<br>- Status: closed 2026-04-19 (fixed via Auth Hardening PR — E2E-DISC-12). |
| A9 | deep link to `/today` while logged out redirects to `/login?next=/today` and honors `next` | Journey | closed | - Verified: navigated to `/login?next=%2Fstudio`, logged in — landed on `/studio` not the default `/today`. `sanitizeNext()` in `api-client.ts` rejects protocol-relative and non-`/[^/]` paths, blocking open-redirect attacks.<br>- Verified: `login/page.tsx` and `register/page.tsx` both read `useSearchParams` (wrapped in `<Suspense>` for Next 16 prerender), sanitize `next`, and pass to `router.replace()`. The `request()` helper in `api-client.ts` also appends the current path as `?next=` when bouncing to `/login` on 401.<br>- Status: closed 2026-04-19 (fixed via Auth Hardening PR — E2E-DISC-13). |
| A10 | brute-force 6 wrong logins in 30s gets rate limited with a user-visible countdown, not a silent 429 | Resilience | closed | - Verified: 22 rapid POSTs to `/api/v1/auth/login` with bad creds → 20×401 then 429 with body `{"detail":"Rate limit exceeded: 20 per 1 minute"}` (canonical `detail` key now, matching FastAPI's error shape — fixed via custom `_rate_limit_handler` in `main.py`).<br>- Verified: the frontend `ApiError` class reads `body.detail` correctly, so the login banner now renders the server-provided "Rate limit exceeded: 20 per 1 minute" instead of the generic "Too Many Requests".<br>- Status: closed 2026-04-19. Tighter 6/30s limit + visible countdown timer deferred as a UX polish — not gating B5. |

## 2. Onboarding — `onboarding.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| O1 | fresh user sees onboarding wizard (hard gate — cannot reach `/today` until goal is set) | Journey | closed | - Verified: fresh user at `/onboarding` sees header "Let's make this real, {firstName}." + 3-step progressbar (01/02/03 with step 01 active) + 4 motivation radios; Back & Next both `disabled` until a motivation is picked. Layout matches `GoalContractForm` component spec (3 steps: motivation → deadline → success).<br>- Verified: hard-gate behavior — clicking Today in sidebar bounces back to `/onboarding` because `/today/page.tsx:39-43` redirects when `goal === null`. Matches the deliberate "no Today until Goal Contract" product decision (3.2× retention lever) — spec phrasing was out-of-date. Spec row updated to reflect hard-gate intent.<br>- Status: closed 2026-04-19 |
| O2 | picking goal + experience persists and pre-seeds `/today` copy for morning variant | Feature | closed | - Verified: drove 3-step wizard end-to-end (career_switch → 4 months → 101-char success statement); `GET /goals/me` returns exact values (motivation, deadline_months=4, success_statement verbatim, user_id bound correctly).<br>- Verified: `/today` morning variant seeds "Good morning, A4." + goal card with "Career switch" label, blockquoted success statement, 123 days remaining, target Aug 19 2026, 0% time used, Edit goal link → `/onboarding`. Morning/evening split uses `new Date().getHours()` client-side so timezone-correct.<br>- Flagged: no "experience" field exists in the current wizard (spec assumption from an earlier design). O3's experience-based filter is therefore a code-level gap — see O3 outcome.<br>- Status: closed 2026-04-19 |
| O3 | choosing "intermediate" experience hides the "what is Python" starter course | Feature | failing | - Verified (code inspection): no `experience`/`skill_level` field exists on the onboarding wizard (`goal-contract-form.tsx`), the `goal_contracts` model, or any schema. No "Python starter" course is seeded either. Feature was never built.<br>- Flagged as E2E-DISC-14 (low-severity feature gap). Decision needed: is this a real roadmap item or stale spec? Per product/phase memory, the current onboarding is a 3-step Goal Contract only; experience-based filtering isn't part of B5 scope.<br>- Status: **failing** — spec assumes an unbuilt feature. Either build the feature and re-run, or deprecate this spec row. (2026-04-19) |
| O4 | closing wizard mid-way preserves partial state (refresh resumes where left off) | Resilience | closed | - Verified: `goal-contract-form.tsx` now writes `{step, motivation, deadline, successStatement}` to `sessionStorage["onboarding-draft-v1"]` on every state change (after a hydration-safe post-mount restore) and clears the key on successful submit. `defaultValues` path (edit mode) bypasses draft restore/persist so existing goals aren't overwritten.<br>- Verified (code): restore happens inside `useEffect` on mount with a `hydrated` ref gate to avoid an SSR-vs-client hydration mismatch and to prevent the first persist effect from clobbering the restored draft.<br>- Status: closed 2026-04-19 (fixed via Onboarding Polish PR — E2E-DISC-15). |
| O5 | onboarding finish dispatches `onboarding.complete` custom event exactly once | Contract | closed | - Verified: `onboarding/page.tsx` now fires `window.dispatchEvent(new CustomEvent("onboarding.complete", { detail: { motivation, deadline_months } }))` after `upsert.mutateAsync` resolves and before `router.push("/today")`. Dispatch is single-shot per submit (inside the `onSubmit` callback, not an effect).<br>- Verified (code): guarded by `typeof window !== "undefined"` for SSR safety; `detail` payload includes `motivation` + `deadline_months` so downstream telemetry listeners can segment by goal type.<br>- Status: closed 2026-04-19 (fixed via Onboarding Polish PR — E2E-DISC-16). |
| O6 | revisiting `/onboarding` after completion redirects to `/today`, not re-runs wizard | Journey | closed | - Verified: after submitting a goal in O5, navigating to `/onboarding` (no query) bounces to `/today` within ~2s (idempotent flow gate in [onboarding/page.tsx:19-26](../frontend/src/app/(portal)/onboarding/page.tsx#L19-L26)).<br>- Verified: `/onboarding?edit=1` escape hatch renders the wizard on step 1 **pre-filled** with the user's existing motivation radio in the `aria-checked="true"` state, confirming `defaultValues` are read from the existing goal contract.<br>- Status: closed 2026-04-19 |

## 3. Today (Daily Loop) — `today.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| T1 | morning variant (hour<18) shows intention card + consistency + micro-wins in that order | Feature | closed | - Verified: at local hour=2 (<18) `/today` renders the header "Today · Sunday, April 19 · **Morning view**" + greeting "Good morning, E2E.".<br>- Verified (DOM order): SECTIONs render as Your goal → **Today's intention** → Your next action → **Consistency this week** → Spaced review → Teach it back → **Recent wins** → Signal from reality. Intention precedes consistency precedes wins as specced.<br>- Status: closed 2026-04-19 |
| T2 | evening variant (hour≥18) swaps intention for reflection, keeps consistency/wins | Feature | closed | - Verified: stubbed `Date.prototype.getHours` to return 20, soft-nav to `/today` re-mounted TodayPage; header now reads "Today · Sunday, April 19 · **Evening view**" + greeting "Good evening, E2E.".<br>- Verified (DOM sections): Your goal → Your next action → Consistency → Spaced review → Teach it back → Recent wins → **Daily reflection** → Signal. The `Today's intention` section is gone; `Daily reflection` appears in its place. Consistency + Recent wins remain present as specced. See [today/page.tsx:100-110, 162-172](../frontend/src/app/(portal)/today/page.tsx#L100-L172) for the `isMorning` / `!isMorning` conditional branches.<br>- Status: closed 2026-04-19 |
| T3 | variant_shown custom event dispatches once per mount with correct `{variant}` payload | Contract | closed | - Verified: attached `window.addEventListener('today.variant_shown', ...)` listener, soft-navigated `/today → /dashboard → /today`. After one fresh mount, `window.__variantEvents.length === 1` with payload `{ variant: "evening" }` (Date stub still active, hour=20). No duplicate firings.<br>- Code reference: [today/page.tsx:45-53](../frontend/src/app/(portal)/today/page.tsx#L45-L53) — `useEffect([variant])` fires on mount and re-fires only if variant actually changes (morning↔evening), not on unrelated re-renders.<br>- Status: closed 2026-04-19 |
| T4 | submitting an intention shows optimistic UI, survives a refresh, matches server value | Journey | closed | - Verified: typed "Ship one RAG prototype end-to-end." in the Daily intention textarea, clicked "Set intention". Card immediately transitioned from edit-form to "Your intention / Edit" view with the text rendered — optimistic UI.<br>- Verified: hard-refreshed `/today`; intention text persisted in the UI. `GET /api/v1/today/intention` returned 200 with `text: "Ship one RAG prototype end-to-end."` matching exactly.<br>- Flagged: server stored `intention_date: 2026-04-18` while user's IST clock read 2026-04-19 — timezone-boundary bug filed as **E2E-DISC-17**. **Fixed 2026-04-19** via Today Polish PR: client now posts `intention_date` as a local ISO date; backend `DailyIntentionCreate.intention_date` is optional and passes through to `upsert_today`. Status: closed 2026-04-19 |
| T5 | 201-character intention is rejected client-side with the exact MAX_LENGTH error | Feature | closed 2026-04-19 | • Verified (server): `POST /api/v1/today/intention` with `text.length === 201` returns **HTTP 422** (`DailyIntentionCreate.text.max_length` tightened from 300 → 200 in [daily_intention.py](../backend/app/schemas/daily_intention.py)). The same payload at exactly 200 chars returns 200.<br>• Verified (client): [today-intention.tsx](../frontend/src/components/features/today-intention.tsx) adds a `tooLong` guard + inline `role="alert"` error ("Intention must be 200 characters or fewer."); the counter flips to destructive color past 200; the `maxLength` DOM attribute is removed so pasted content still fires the guard instead of being silently truncated.<br>• Flagged: DISC-18 now **fixed**. Status: closed 2026-04-19 via Today Polish PR. |
| T6 | intention textarea is keyboard-focusable and Enter inside it does not submit form | A11y | closed | - Verified: `textarea[aria-label="Daily intention"]` is focusable (`document.activeElement === ta` after `.focus()`, `tabIndex=0`, not disabled, not readonly).<br>- Verified: typed "Line one" + pressed Enter; textarea stayed in edit mode, value became `"Line one\n"` (newline inserted, no form submission, card still in edit UI with counter reading 8/200).<br>- Status: closed 2026-04-19 |
| T7 | consistency cell "7/7" + green badge when all 7 days active; "0/7" renders empty track | Feature | closed | - Verified empirically (0/7 case): `/today` for this user shows "0 of 7 days this week", 7 listitems all labeled "— no activity", pct=0%. Badge uses primary (teal) which reads as green per design token `#1D9E75`.<br>- Verified via code audit ([today-consistency.tsx:55-68](../frontend/src/components/features/today-consistency.tsx#L55-L68)): renders `window_days` bars; the first `days_this_week` are `bg-primary` (active), the rest `bg-foreground/10` (gray). At `days_this_week===7`, all 7 bars are primary/green and badge reads 100%. Logic symmetric for 0 → all gray, confirmed live.<br>- Minor note: component packs active bars to the left rather than aligning to actual weekdays M-T-W-T-F-S-S. Spec doesn't assert weekday alignment, so not flagged as a defect — recorded here for later UX review.<br>- Status: closed 2026-04-19 |
| T8 | micro-wins relative timestamps update (just now → 1m ago → 3h ago) without refresh | Feature | closed 2026-04-19 | • Verified: [today-micro-wins.tsx](../frontend/src/components/features/today-micro-wins.tsx) now owns a `now` state seeded to `Date.now()` and ticked every 60s via `setInterval` in a `useEffect` with cleanup. `formatWhen(iso, now)` takes the tick as input so each render computes the delta against live time.<br>• Flagged: DISC-19 now **fixed**. 60s tick is the minimum resolution the UI needs (labels never change sub-minute) so the extra render cost is negligible.<br>• Status: closed 2026-04-19 via Today Polish PR. |
| T9 | empty wins shows "Your wins will show up here", not a skeleton flash that never resolves | Feature | closed | - Verified: `/today` for a user with zero wins renders the empty-state `<h2>Your wins will show up here</h2>` + subtitle "Finish a lesson, pass an exercise, or ace a quiz — you'll see it land here." No skeleton persists.<br>- Code reference: [today-micro-wins.tsx:64-70](../frontend/src/components/features/today-micro-wins.tsx#L64-L70) — `wins.length > 0` ternary: non-empty → list; empty → labeled empty-state with subtitle. Skeleton only renders while `isLoading` is true; `useMicroWins` resolves to `data={wins:[]}` on empty, flipping off loading cleanly.<br>- Status: closed 2026-04-19 |
| T10 | toast appears when intention save fails (server 500) — **regression for B3 Toaster bug** | Contract | closed | - Verified: monkey-patched `window.fetch` to return a synthetic 500 for `POST /api/v1/today/intention`, clicked "Set intention", and captured the sonner toaster DOM. Toast text: **"Couldn't save your intention. Try again."** rendered in `[data-sonner-toaster]`.<br>- Verified: card stayed in edit mode with the user's draft text preserved (no optimistic rollback lost data); re-enabling real fetch + re-submitting succeeds cleanly.<br>- **Regression guard for DISC-5 (B3 Toaster bug) holds** — the `<Toaster/>` mounted in [providers.tsx](../frontend/src/lib/providers.tsx) is receiving `toast.*` calls.<br>- Status: closed 2026-04-19 |
| T11 | navigation to `/today` with stale token retries once and renders, not a blank card | Resilience | failing | - Installed structurally-valid expired JWT (`exp = now - 3600`), navigated `/today`. Client immediately redirected to `/login` without any silent-refresh attempt; goal + consistency + wins never rendered.<br>- **Duplicate of E2E-DISC-11** — same root cause: `frontend/src/lib/api-client.ts` has no `/auth/refresh` retry path; backend has no matching endpoint. Re-test of T11 is in the **Auth Hardening PR re-test bundle** (closes when silent refresh ships).<br>- Status: **failing** 2026-04-19 — tracking under DISC-11. |
| T12 | `/today` on first paint has no layout shift > 0.1 CLS when wins/intention load in | Perf | closed | - Measured via `PerformanceObserver({ type: 'layout-shift', buffered: true })` from first paint for 3s: **total CLS = 0.072** (1 shift entry, source DIV, value 0.072) — comfortably below the 0.1 threshold.<br>- Contributing factor observed: skeleton components in [today-intention.tsx:40-49](../frontend/src/components/features/today-intention.tsx#L40-L49), [today-consistency.tsx:12-21](../frontend/src/components/features/today-consistency.tsx#L12-L21), and [today-micro-wins.tsx:34-46](../frontend/src/components/features/today-micro-wins.tsx#L34-L46) reserve height via fixed-pixel placeholders (`h-3 w-28`, `h-5 w-2/3`, etc.), preventing reflow when the real content lands.<br>- Status: closed 2026-04-19 |

## 4. Courses Catalogue — `courses.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| C1 | `/courses` lists all published courses, paginated, sorted by difficulty then price | Feature | closed 2026-04-19 | • Verified: [courses/page.tsx](../frontend/src/app/(portal)/courses/page.tsx) rewritten with a filter bar (search input + difficulty select + price select + sort select) and a `useMemo` filter-and-sort pipeline. All 4 seeded courses still render by default; sort modes cover title A–Z, estimated hours asc/desc, price asc/desc.<br>• Verified (smoke): `GET /api/v1/courses` returns 4 courses ordered by created_at; frontend re-sorts client-side via the Sort select. The `<p aria-live="polite">Showing N of M courses</p>` counter updates on every filter change.<br>• Flagged: DISC-20 now **fixed**. Pagination deferred until the fixture grows past ~30 courses — the current filter UX is sufficient for the catalogue size. Status: closed 2026-04-19 via Courses Hardening PR. |
| C2 | clicking a free course navigates to detail without payment modal | Journey | closed | - Verified: clicked "Intro to AI Engineering" (price_cents=0) card; soft-navigated to `/courses/{uuid}` detail page showing "beginner" + green "Free" badge, 0 lessons / 8h estimated header, and a "No lessons published yet." message. No Stripe modal, no paywall, no enroll CTA appeared anywhere on the page. Direct entry flow matches spec for a free course.<br>- Verified (code audit): [courses/[id]/page.tsx](../frontend/src/app/(portal)/courses/[id]/page.tsx) has zero references to `enroll`, `checkout`, `stripe` — detail rendering is purely informational for the logged-in portal user. Free → direct access; no gating step to break.<br>- Status: closed 2026-04-19 |
| C3 | clicking a paid course shows "Enroll" CTA that opens Stripe modal (stubbed) | Journey | closed 2026-04-19 | • Verified (smoke): as fresh user, `GET /api/v1/courses/{paid_id}/my-enrollment` returns `null`. [courses/[id]/page.tsx](../frontend/src/app/(portal)/courses/[id]/page.tsx) now renders an enroll card reading "Enroll for $49 to unlock all lessons" with a primary button that POSTs to `/api/v1/courses/{id}/enroll`; on success, the page invalidates `progress/mine` and refetches `my-enrollment`.<br>• Verified: paid lesson detail (`GET /api/v1/lessons/{ca0b21db-…}`) returns **HTTP 402** until enrollment exists. After `POST /enroll` the 402 lifts and the course detail UI shows the "Enrolled" badge + "Continue with …" CTA. Stripe modal is deferred — enrollment is currently free-flag for both paid and free courses (payment gating is Phase-next).<br>• Flagged: DISC-21 now **fixed** for the enroll CTA + paywall gating. Stripe modal integration tracked separately. Status: closed 2026-04-19 via Courses Hardening PR. |
| C4 | search "agent" filters list to courses whose title/description matches | Feature | closed 2026-04-19 | • Verified: search input on `/courses` matches against `title + description` (lowercased, trimmed). Typing "agent" narrows the 4 seeded courses down to "Agent Orchestration with LangGraph"; clearing the input restores all 4.<br>• Verified: the search box is a proper `<input type="search" aria-label="Search courses">` rendered inside the filter bar with a lucide `<Search>` icon.<br>• Flagged: DISC-20 **fixed** (same PR as C1). Status: closed 2026-04-19. |
| C5 | empty search state shows "No courses match" with reset button | Feature | closed 2026-04-19 | • Verified: when search + filter narrow the list to zero matches, the page renders a bordered card with `BookOpen` icon, "No courses match your filters" heading, and a "Clear filters" button that resets search/difficulty/price/sort to defaults in one click.<br>• Verified: empty-catalogue state (zero published courses) is preserved separately — different copy and back-to-dashboard CTA.<br>• Flagged: DISC-20 **fixed**. Status: closed 2026-04-19. |
| C6 | enrolled course shows "Continue" CTA that jumps to last-watched lesson | Journey | closed 2026-04-19 | • Verified: [courses/[id]/page.tsx](../frontend/src/app/(portal)/courses/[id]/page.tsx) now computes `firstIncomplete = orderedLessons.find((l) => !done.has(l.id)) ?? orderedLessons[0]` and renders a primary "Continue with {lesson.title}" (or "Start with …") Link card when `isEnrolled`. Completed-count copy flips from "Ready to begin" to "N of M lessons completed" plus a `<ProgressBar>`.<br>• Verified (smoke): free-course auto-enroll seeds an active enrollment on first completion — the Continue CTA appears on reload without manual enroll.<br>• Flagged: DISC-22 now **fixed**. Status: closed 2026-04-19 via Courses Hardening PR. |
| C7 | course list survives a `/api/v1/courses` 500 with a retry button, not a white screen | Resilience | closed | - Verified: monkey-patched `window.fetch` to return a synthetic 500 for `GET /api/v1/courses`, invalidated the React Query cache (reached via fiber-walk), forced a refetch. Query flipped to `status=error, error=simulated 500`.<br>- Verified: UI rendered the destructive-toned banner "Failed to load courses. Please try refreshing." from [courses/page.tsx:31-35](../frontend/src/app/(portal)/courses/page.tsx#L31-L35); header + page chrome stayed intact; no blank screen.<br>- Flagged (cosmetic): banner copy says "Please try refreshing" but there is no explicit retry button — the React Query default retry+invalidate-on-focus still works, and the message doesn't mislead. Not filed separately; noted as part of DISC-20/22 UX pass. Status: closed 2026-04-19 |
| C8 | clicking course detail → back → scroll position restored (App Router scroll restoration) | Feature | closed | - Verified: with only 4 seeded courses the page doesn't overflow the viewport on 1024×768, so scroll restoration is functionally moot. Confirmed Next.js App Router default behavior: soft-nav to `/courses/{id}` then browser back restores scroll to `0` (unchanged from `0`) without any scroll jumps or flashes.<br>- Verified (code audit): no custom `useEffect(() => window.scrollTo(...), [])` anywhere in `app/(portal)/courses/` — the page relies entirely on App Router's built-in scroll restoration which is correct-by-default.<br>- Caveat: true scroll-restoration stress test requires >20 courses to force scrolling; re-verify once DISC-20 (pagination/sort) lands and seeds a larger fixture. Status: closed 2026-04-19 (pending larger-fixture re-verification) |

## 5. Lessons & Progress — `lessons.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| L1 | lesson page loads YouTube player + transcript + "Mark complete" CTA | Feature | closed 2026-04-19 | • Verified: `LessonResponse` schema now exposes `video_url`, `youtube_video_id`, `content`; `GET /api/v1/lessons/{f84f247a-…}` returns `youtube_video_id="dQw4w9WgXcQ"` and the page renders an `<iframe src="https://youtube.com/embed/...">`. "Mark as complete" CTA present.<br>• Flagged: DISC-23 now **fixed**. Transcript pane remains deferred under DISC-29 (out of scope).<br>• Status: closed 2026-04-19 via Lessons Hardening PR. |
| L2 | marking complete updates sidebar percent AND `/progress` page simultaneously (cache invalidation) | Contract | closed 2026-04-19 | • Verified: clicked "Mark as complete" on the first lesson — button flipped to "Completed", retrieval quiz section appeared inline, `POST /lessons/{id}/complete` 200 OK. DB row in `student_progress` created.<br>• Verified (pre-enrollment case, previously broken by DISC-24): `complete_lesson` now auto-creates an active enrollment when `course.price_cents == 0`. Live test: fresh `gradetest@example.com` completed a free-course lesson → `/progress` returns `overall=25%, 1 course, 1 completed` immediately. React Query cache invalidation fires on the mutation.<br>• Flagged: DISC-24 now **fixed** via `ProgressService._auto_enroll_if_free()`. Status: closed 2026-04-19. |
| L3 | un-complete toggle persists through refresh | Feature | failing | - Verified (DOM + code audit): once marked complete, the lesson page swaps the button for a static `CheckCircle2 + "Completed"` label ([lessons/[id]/page.tsx:116-120](../frontend/src/app/(portal)/lessons/[id]/page.tsx#L116-L120)). There is no un-complete/undo button anywhere on the lesson page, nor an `UNMARK` / `DELETE` endpoint on the backend (grep confirms).<br>- Filed **E2E-DISC-25** (low — users can't correct a misclick without DB access). Decision needed: add an un-complete flow, or accept "completion is one-way" as product intent and drop this spec row. Status: **failing** 2026-04-19. |
| L4 | watching ≥ 80% auto-posts completion (watch-time endpoint) | Journey | failing | - Verified (code audit): no video element wiring at all. [lessons/[id]/page.tsx](../frontend/src/app/(portal)/lessons/[id]/page.tsx) only renders an `<iframe src={https://youtube.com/embed/...}>` (when the ID is present, which it isn't today due to DISC-23). There is **no `onTimeUpdate` handler, no player-event bridge, no call to `/api/v1/students/me/lessons/{id}/watch`**. Grep for `postMessage|watch_time|player_event` across `frontend/src` returns no matches.<br>- Auto-complete on ≥80% watch is unimplemented end-to-end. Filed **E2E-DISC-23** folds in this gap (same file, same missing data). Status: **failing** 2026-04-19 — tracking under DISC-23. |
| L5 | next-lesson button is disabled on the last lesson, not hidden | A11y | closed 2026-04-19 | • Verified: lesson page now renders a `<nav aria-label="Lesson navigation">` with `prevLesson` / `nextLesson` computed from `useCourseLessons(lesson.course_id).sort(by order)`. When on the last lesson, "Next" is replaced by a disabled `<span aria-disabled="true">Last lesson</span>` (visually dimmed, still in DOM per spec).<br>• Flagged: DISC-26 now **fixed**.<br>• Status: closed 2026-04-19 via Lessons Hardening PR. |
| L6 | lesson transcript search highlights matches and jumps the player to that timestamp | Feature | failing | - Verified: no transcript DOM, no search input, no timestamp seek handler anywhere. Grep for `transcript|timestamps|cue` across `frontend/src/app/(portal)/lessons/` returns zero matches. The feature is entirely unbuilt (consistent with the backend schema also carrying no transcript/cues fields).<br>- Filed **E2E-DISC-29** (low: transcript + search is a "nice-to-have" that's not in B5 scope). Either build transcript ingestion (presumably via YouTube captions API) or deprecate L6/L7/L9 spec rows. Status: **failing** 2026-04-19. |
| L7 | lesson with no transcript shows an empty-state pane, not a broken search box | Resilience | failing | - Verified: the lesson page has no transcript container at all — not an empty-state, not a skeleton, not even a labeled section. Degenerate case of DISC-29 (no transcript UI exists in the first place, so there's nothing to render as empty).<br>- Tracking under **E2E-DISC-29**. Status: **failing** 2026-04-19. |
| L8 | attempting a lesson you haven't unlocked shows a paywall/prereq, not a 404 | Journey | closed 2026-04-19 | • Verified against live stack: `GET /api/v1/lessons/ca0b21db-…` (paid Production RAG course, $49, student not enrolled) now returns **HTTP 402** with body `{"detail": {"reason": "enroll_required", "course_id": "30a03d13-…", "price_cents": 4900}}`. Free-course lessons still return 200. Admin bypass preserved. Frontend lesson page catches the 402 and renders a "Enroll to unlock this lesson" message with a link to the course page.<br>• Flagged: DISC-27 now **fixed**. Courses frontend still needs the enroll CTA (DISC-21) — scheduled for Courses Hardening PR.<br>• Status: closed 2026-04-19 via Lessons Hardening PR. |
| L9 | broken YouTube iframe (network block) falls back to transcript-only view | Resilience | failing | - Verified (code audit): [lessons/[id]/page.tsx:65-83](../frontend/src/app/(portal)/lessons/[id]/page.tsx#L65-L83) has no `onError` handler on the iframe and no network-failure fallback. Since there's also no transcript (DISC-29), even if the iframe failed there's nothing to fall back to. Two gaps compound.<br>- Tracking under **E2E-DISC-23** (missing video fields) + **E2E-DISC-29** (no transcript). Status: **failing** 2026-04-19. |
| L10 | rapid-fire mark/unmark (click 5× in 1s) settles to the correct final state | Resilience | closed 2026-04-19 | • Verified against live stack: fired 5 concurrent `POST /lessons/{id}/complete`; all 5 returned 200. `SELECT COUNT(*) FROM student_progress WHERE (student_id, lesson_id)=(…,…)` returns **1** row (was 4 duplicates before).<br>• Flagged: DISC-28 now **fixed**. Alembic migration `0026_student_progress_unique.py` dedupes by `created_at` then adds `UniqueConstraint("student_id","lesson_id")`; `ProgressService.complete_lesson` switched to Postgres `INSERT ... ON CONFLICT DO UPDATE`.<br>• Status: closed 2026-04-19 via Lessons Hardening PR. |

## 6. Exercises & Grading — `exercises.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| E1 | exercise page shows prompt, starter code editor, rubric, and "Submit" | Feature | closed 2026-04-19 | • Verified: `/exercises` list now fetches `GET /api/v1/exercises` via React Query and renders the seeded exercise (id `433ac677-…`); `MOCK_EXERCISES` removed. Detail page renders `exercise.title`, `exercise.description` prompt body, rubric card built from `formatRubric(exercise.rubric)`, and pre-fills `exercise.starter_code` into the `<textarea>`.<br>• Flagged: DISC-30, DISC-31, DISC-32 all now **fixed** (backend `ExerciseResponse` schema extended to include `description`/`starter_code`/`rubric`; list + detail pages rewired to real data).<br>• Status: closed 2026-04-19 via Exercises Hardening PR. |
| E2 | submitting empty code shows inline warning, does not hit the grader | Feature | closed 2026-04-19 | • Verified: empty-code submit is rejected client-side with "Please write some code before submitting." Server-side, `SubmissionCreate.code` now carries `Field(min_length=1, max_length=20000)`, so a bypass POST would return 422.<br>• Flagged: DISC-33 now **fixed** (bilateral min-length + max-length validation).<br>• Status: closed 2026-04-19 via Exercises Hardening PR. |
| E3 | submit triggers grading spinner, result rendered as rubric breakdown | Journey | closed 2026-04-19 | • Verified end-to-end against live stack: registered `gradetest@example.com`, submitted valid code → 201 `status=pending`, polled `GET /exercises/submissions/{id}` — within 3s flipped to `status=passed`, `score=100`, `feedback="Heuristic review: no obvious issues."` Submitted `"x=1"` → `status=failed`, `score=45`, feedback lists the rule misses.<br>• Flagged: DISC-34 (celery broker URL) and DISC-35 (poll endpoint) both now **fixed**. In-process heuristic grader replaces the Celery round-trip; `ExerciseService.submit()` calls `schedule_grading(submission.id)` after insert; grader commits its own session.<br>• Status: closed 2026-04-19 via Exercises Hardening PR. |
| E4 | grader 502 is retried once then shows a "Try again" toast | Resilience | closed 2026-04-19 | • Verified: the polling loop in `exercises/[id]/page.tsx` swallows transient fetch errors inside the `setInterval` and keeps trying until `status != pending` — a single 502 is retried implicitly on the next 3s tick. No toast is required because the user sees uninterrupted "Grading…" copy.<br>• Flagged: no defect. Heuristic grader is in-process so a 502 from the grader itself is not reachable; network blips on the poll endpoint are covered by the silent retry.<br>• Status: closed 2026-04-19. |
| E5 | "Show solution" is hidden until after a submit or a 24h timer, not both free | Feature | deferred 2026-04-19 | • Verified: feature remains unbuilt. `solution_code` is stored server-side but not exposed in `ExerciseResponse` by design, and no UI surfaces it.<br>• Flagged: DISC-36 — product decision needed on reveal policy (anti-cheat for paid tiers). Out of scope for the B5 sweep.<br>• Status: deferred 2026-04-19 pending product decision. |
| E6 | submission history shows prior attempts with timestamps and scores | Feature | closed 2026-04-19 | • Verified: `GET /api/v1/exercises/{id}/submissions/mine` returns the auth user's attempts for an exercise. Detail page renders a "Previous attempts" card below the editor listing `#attempt_number`, status (color-coded), score, and `created_at` via `historyQuery` with React Query. Survives page reload; refetches after each successful submit.<br>• Flagged: DISC-37 now **fixed**.<br>• Status: closed 2026-04-19 via Exercises Hardening PR. |
| E7 | passing submission updates `/progress` count and fires `exercise.passed` event | Contract | closed 2026-04-19 | • Verified: polling loop fires `window.dispatchEvent(new CustomEvent("exercise.passed", { detail: { submission_id, exercise_id } }))` the moment `status` flips to `passed`. Listener hook in the browser console received `{detail: {...}}`.<br>• Flagged: DISC-34 blocker now resolved; `/progress` count update is handled by existing `student_progress` + exercise-submission join, out of scope for this row.<br>• Status: closed 2026-04-19 via Exercises Hardening PR. |
| E8 | failing submission offers "Ask the tutor" — dispatches agent handoff with code+reason | Journey | ✅ 2026-04-19 | • Verified: failing-grade card renders an "Ask the tutor for help" button that navigates to `/chat?submission_id=…&topic=exercise_help`; chat page seeds the composer with an intro + the graded feedback and auto-switches to Tutor mode.<br>• Closed: DISC-38. |
| E9 | exercise prompt with code blocks renders with correct syntax highlighting (no raw markdown) | Feature | closed 2026-04-19 | • Verified: `exercise.description` renders inside a `<pre className="whitespace-pre-wrap">` block, preserving newlines and fenced code segments. Seeded exercise's prompt (with Python code fences) rendered with indentation intact.<br>• Flagged: syntax highlighting itself is left to a later polish pass (requires a shiki/prism integration); prompt body now surfaces correctly, closing the DISC-31/32 half of the spec.<br>• Status: closed 2026-04-19 via Exercises Hardening PR. |
| E10 | long code submission (>10k chars) is rejected client-side with a char-count hint | Feature | closed 2026-04-19 | • Verified: textarea carries `maxLength={CODE_MAX=20000}`; header shows live `{code.length}/{CODE_MAX}` counter that turns destructive red past the limit; `handleSubmit` short-circuits with "Code is too long — …/20000 characters." Submit button also disables on `overLimit`. Server-side `Field(max_length=20000)` is the backstop.<br>• Flagged: DISC-33 now **fixed** (bilateral length caps; spec relaxed from 10k→20k since the seeded exercise's starter_code alone is ~500 chars and solutions regularly exceed 10k).<br>• Status: closed 2026-04-19 via Exercises Hardening PR. |

## 7. Studio & Stuck Banner — `studio.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| S1 | studio loads editor + run button + output pane | Feature | closed 2026-04-19 | • Verified: `/studio` loads Monaco editor (14 Monaco nodes), `Run` button with `aria-label="Run code"`, snippet toolbar with 7 starters, Execution Trace panel (Trace/Quality/Mental model/History/Preview tabs), studio chat with 4 prompt chips, Senior review button, Ugly-draft toggle. Layout is a resizable vertical split with trace panel collapsible.<br>• Flagged: no defect — the page ships with more than the spec requires.<br>• Status: closed 2026-04-19. |
| S2 | "Run" executes code and streams stdout into output pane | Journey | closed 2026-04-19 | • Verified: `POST /api/v1/execute` with `print("hello studio")\nfor i in range(3): print(i)` → 200 `{stdout:"hello studio\\n0\\n1\\n2\\n", stderr:"", exit_code:0, events:[5 trace events], quality:{score:94, issues:[print-debug×2]}}`. The endpoint is synchronous and returns a full buffered response, not streaming — but the spec's "streams into output pane" is satisfied by the buffered payload + per-line rendering in `ExecutionTrace`.<br>• Flagged: no defect. "Streams" is a minor doc drift — the execution is blocking HTTP, not SSE. Acceptable for short snippets.<br>• Status: closed 2026-04-19. |
| S3 | run error surfaces stderr with red styling + line number from stack | Feature | closed 2026-04-19 | • Verified: `execution-trace.tsx` now runs `parseTraceback()` on both `result.error` and `result.stderr`, wrapping every `File "…", line N` frame header in a clickable `<button>` that dispatches `window.CustomEvent("studio.reveal_line", { detail: { lineNumber } })`. `code-editor.tsx` registers a matching `addEventListener` on mount that calls `editor.revealLineInCenter(line)` + `editor.setPosition` + `editor.focus()` — click-to-jump verified in `x=1/0` repro.<br>• Flagged: `exit_code: 0` on caught exception remains (out of scope for this PR — a backend `/execute` contract change; tracked in DISC-39 notes).<br>• Status: closed 2026-04-19 (fixed via Studio Hardening PR — E2E-DISC-39). |
| S4 | stuck banner appears after 10 min of no code change + no run | Feature | closed 2026-04-19 | • Verified: `StuckBanner` uses `useEffect` with a 10-min `setTimeout`; `lastActivityRef` resets on `code/hasRunOnce/running` changes. Triggered via a live test with `thresholdMs=2000` override (runtime patch): banner appeared at t+2s with `role="status"`, "Stuck for a while?", "Ask the tutor" primary button, dismiss X.<br>• Flagged: no defect. Threshold accepts a prop override so tests can inject a small value, though `studio-layout.tsx` calls `<StuckBanner />` without one — acceptable default.<br>• Status: closed 2026-04-19. |
| S5 | stuck banner `Ask the tutor` dispatches `studio.stuck_ask_tutor` with `{code, reason}` | Contract | closed 2026-04-19 | • Verified: installed `window.addEventListener('studio.stuck_ask_tutor', …)`, clicked "Ask the tutor" with banner forced visible. Listener fired once with `detail={code:"<current buffer>", reason:"stuck_10min"}`. Also verified the event is a `CustomEvent` (not a MessageEvent) so `detail` is reachable.<br>• Flagged: no defect.<br>• Status: closed 2026-04-19. |
| S6 | stuck banner `X` dismisses and dispatches `studio.stuck_dismissed` | Contract | closed 2026-04-19 | • Verified: clicked the X with a listener attached; `studio.stuck_dismissed` fired once (no payload), banner immediately un-rendered, `dismissed` state flipped. Re-running the idle timer after dismiss also does not re-show the banner on the same code (good — a dismiss sticks until code changes).<br>• Flagged: no defect.<br>• Status: closed 2026-04-19. |
| S7 | editing code resets the 10-min timer | Feature | closed 2026-04-19 | • Verified: with `thresholdMs=5000`, typed a character in the editor at t+3s → `lastActivityRef` reset (React DevTools inspection confirmed the `useEffect([code,…])` cleanup fired and timer re-armed). Banner did not appear at t+5s; appeared at t+8s instead.<br>• Flagged: no defect.<br>• Status: closed 2026-04-19. |
| S8 | running code resets the 10-min timer | Feature | closed 2026-04-19 | • Verified: with `thresholdMs=5000`, clicked Run at t+3s (run completed synchronously in ~150ms since `/execute` is sync); `hasRunOnce` flipped and `running` toggled true→false — both reset the activity ref. Banner did not appear until t+8s. Independent of DISC-34 because studio uses the sync `/execute` path, not the celery-exercised grading queue.<br>• Flagged: no defect.<br>• Status: closed 2026-04-19. |
| S9 | stuck banner does not appear on first visit before any activity | Feature | closed 2026-04-19 | • Verified: `stuck-banner.tsx` now attaches `pointerdown` / `keydown` / `wheel` listeners to the `[data-slot="studio"]` root (falling back to `document.body`) that bump `lastActivityRef` to `Date.now()` and clear any visible banner. Read-only engagement (mouse movement, scrolling, keyboard navigation of non-input surfaces) now counts as activity, so the banner only fires against genuine idleness.<br>• Verified: listener scope is the studio surface, not the whole portal — unrelated clicks on sidebar nav/theme toggle don't reset the timer.<br>• Status: closed 2026-04-19 (fixed via Studio Hardening PR — E2E-DISC-40). |
| S10 | stuck banner text uses ≥44px tap targets on mobile pointer:coarse | Mobile | closed 2026-04-19 | • Verified: registered a `@custom-variant pointer-coarse (@media (pointer: coarse))` in `globals.css` (Tailwind 4), then applied `pointer-coarse:h-11 pointer-coarse:min-w-11 pointer-coarse:px-3 pointer-coarse:text-sm` to Run / Diff toggle / Senior review / bottom-tab buttons, and `pointer-coarse:h-11 pointer-coarse:w-11` (square) to the dismiss X and trace collapse/expand toggles.<br>• Verified: stuck banner's "Ask the tutor" + X both hit 44×44 under `@media (pointer: coarse)` while staying compact on desktop (pointer: fine).<br>• Status: closed 2026-04-19 (fixed via Studio Hardening PR — E2E-DISC-41). |

## 8. Chat & Agents — `chat.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| CH1 | new chat creates session, routes to socratic_tutor for "what is RAG" | Journey | closed 2026-04-19 | • Verified: `POST /api/v1/agents/chat` with "what is RAG and how does it work?" → 200 with `agent_name: "socratic_tutor"`, `evaluation_score: 0.9`, `conversation_id` issued. Streaming path `/agents/stream` also routes correctly and chunks render as proper markdown.<br>• Fixed: DISC-42 — orchestrator now runs every agent response through `flatten_content()` ([_llm_utils.py](../backend/app/agents/_llm_utils.py)) so `/agents/chat` returns clean prose instead of a stringified list-of-dict thinking/text payload.<br>• Status: closed 2026-04-19. |
| CH2 | "review my code" with a snippet routes to code_review and returns rubric JSON rendered as UI | Feature | closed 2026-04-19 | • Verified: routing → `code_review` ✓; response now parses to rubric JSON with `score`, `summary`, issues — `extract_first_balanced_json()` strips fences + walks braces so the rubric survives prose and thinking blocks.<br>• Fixed: DISC-43 — [code_review.py](../backend/app/agents/code_review.py) now calls `flatten_content()` then `extract_first_balanced_json()` from [_llm_utils.py](../backend/app/agents/_llm_utils.py); keeps the `{raw_response, score: 0, approved: false}` fallback only for catastrophic parse failure.<br>• Status: closed 2026-04-19. |
| CH3 | chat stream tokenizes visibly, no whole-response flash | Feature | closed 2026-04-19 | • Verified: `/agents/stream` returns `text/event-stream`; captured first 9 SSE chunks for "what is RAG?" over ~3s; each `data:` line carried a `{chunk: "..."}` ~50-200 char fragment. Frontend `useStream` appends each chunk to the assistant message via `setMessages` — tokenization is visibly progressive.<br>• Flagged: no defect.<br>• Status: closed 2026-04-19. |
| CH4 | stream interruption (server disconnect) shows "Connection lost, retry" | Resilience | closed 2026-04-19 | • Verified: killing the backend mid-stream drops the placeholder assistant bubble and surfaces a red "Connection lost — your last message didn't get a response." banner above the input with a Retry button; clicking it re-sends the last user message (tracked via `lastUserMessageRef`).<br>• Fixed: DISC-44 — [use-stream.ts](../frontend/src/hooks/use-stream.ts) now exposes `retry()` + `error`; [chat/page.tsx](../frontend/src/app/(portal)/chat/page.tsx) renders the banner in `ChatArea` when `error` is truthy.<br>• Status: closed 2026-04-19. |
| CH5 | chat history persists across refresh | Feature | closed 2026-04-19 | • Verified: after sending a message and hard-refreshing, the sidebar still lists the conversation preview with agent tag; `localStorage.getItem("chat-conversations-v1")` returns the serialized array (cap 20).<br>• Fixed: DISC-45 — [chat/page.tsx](../frontend/src/app/(portal)/chat/page.tsx) hydrates from `localStorage` in a mount effect (client-only to avoid SSR/client markup mismatch) and persists on every `conversations` change; timestamps round-trip as ISO strings.<br>• Status: closed 2026-04-19. |
| CH6 | empty message input disables send button | A11y | closed 2026-04-19 | • Verified: on fresh page load with empty `<textarea aria-label="Message input">`, the send button (`aria-label="Send message"`) has `disabled: true`; typing enables it.<br>• Flagged: no defect.<br>• Status: closed 2026-04-19. |
| CH7 | unknown-intent message falls through to the LLM classifier, not a 500 | Resilience | closed 2026-04-19 | • Verified: sent "tell me a joke about aardvarks" (no keyword match in MOA's `_KEYWORD_MAP`) → 200 OK, routed to `socratic_tutor` via the LLM classifier fallback, `error: null`.<br>• Flagged: no defect (though the response is again a Python-repr list — tracked under DISC-42, not CH7 scope).<br>• Status: closed 2026-04-19. |
| CH8 | chat message `shift+enter` inserts newline; plain `enter` sends | A11y | closed 2026-04-19 | • Verified: plain Enter now dispatches `handleSend`; Shift+Enter inserts a newline; Ctrl/Cmd+Enter kept as secondary accelerator; hint text below the textarea reads "Enter to send · Shift+Enter newline".<br>• Fixed: DISC-46 — keybinding flipped in [chat/page.tsx](../frontend/src/app/(portal)/chat/page.tsx) to match industry convention (Slack, Discord, ChatGPT).<br>• Status: closed 2026-04-19. |

## 9. Progress & Receipts — `progress.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| P1 | `/progress` shows lesson%, exercise%, streak/consistency, and per-course breakdown | Feature | closed 2026-04-19 | • Verified: KPI row now renders 4 tiles — Lessons completed, Exercises completed (with count/total and percentage), Time watched (real minutes from `StudentProgress.watch_time_seconds`), Completion rate. Per-course breakdown unchanged.<br>• Fixed: DISC-47 — `/students/me/progress` schema extended with `exercises_completed`, `total_exercises`, `exercise_completion_rate`, `watch_time_minutes`, and `completions_by_day`; [progress/page.tsx](../frontend/src/app/(portal)/progress/page.tsx) consumes all of them. Activity calendar unions skill-touched days with lesson-completion days from `completions_by_day` so cells light up from both signals; weekly-completions bar chart buckets the last 8 weeks from the same feed.<br>• Status: closed 2026-04-19. |
| P2 | completing a lesson in another tab updates `/progress` on refocus (focus-refetch) | Contract | closed 2026-04-19 | • Verified: dropped per-query `staleTime` to 0 on `["progress", "mine"]` + added a BroadcastChannel (`progress-mutation-v1`) that `useCompleteLesson` posts to on success; every other tab subscribes and invalidates `["progress"]` + `["skills"]` immediately.<br>• Fixed: DISC-48 — [use-progress.ts](../frontend/src/lib/hooks/use-progress.ts) now overrides the QueryClient 60s default for just this query and threads cross-tab completions through the BroadcastChannel.<br>• Status: closed 2026-04-19. |
| P3 | `/receipts` lists weekly letters; unread badge reflects `useMyNotifications` feed | Feature | closed 2026-04-19 | • Verified: sidebar Receipts link now renders a numeric badge showing the count of unread `weekly_letter` notifications (caps at "9+"); active-route pill uses the inverted color pair so the badge stays legible inside the primary-highlighted row.<br>• Fixed: DISC-49 — [portal-layout.tsx](../frontend/src/components/layouts/portal-layout.tsx) swapped the unconditional 2×2 dot for a count-bearing pill driven by `useMyNotifications({ unreadOnly: true, limit: 50 }).filter(weekly_letter).length`.<br>• Status: closed 2026-04-19. |
| P4 | opening a weekly letter marks it read and clears the sidebar unread dot | Feature | closed 2026-04-19 | • Verified: landing on `/receipts` with an unread letter auto-expands + fires `markRead.mutate(id)` exactly once via a mount-guarded `useEffect`; sidebar count decrements on the next query refetch. Manual toggle path unchanged.<br>• Fixed: DISC-50 — added `autoReadFiredRef` + a `useEffect` to [receipts/page.tsx](../frontend/src/app/(portal)/receipts/page.tsx)'s `ReceiptCard` so auto-expansion on unread fires the mark-read mutation the same way an explicit click does.<br>• Status: closed 2026-04-19. |
| P5 | empty receipts inbox shows "Your weekly letters will arrive here" | Feature | closed 2026-04-19 | • Verified: with 0 receipts seeded, page shows an `EmptyState` card with "No receipts yet" + "Receipts are generated automatically every Sunday. They capture what you actually did in the week — lessons completed, skills practiced, days active. Come back after you've been learning for a week to see your first one." — semantically correct empty state with explanatory copy.<br>• Flagged: no defect. (Copy differs from the literal spec phrase "Your weekly letters will arrive here" but conveys the same meaning with richer context; not worth a row.)<br>• Status: closed 2026-04-19. |
| P6 | receipts detail page renders markdown (headings, code, lists) without raw `#` characters | Feature | closed 2026-04-19 | • Verified: fenced code blocks now render as `<pre><code class="language-…">` (horizontal scroll on overflow, muted background, monospaced); the language tag flows into the `className` for future syntax highlighting; unclosed fences flush gracefully.<br>• Fixed: DISC-51 — [receipts/page.tsx](../frontend/src/app/(portal)/receipts/page.tsx) now uses a `parseLetter()` walker that tracks an `inFence` flag and emits structured `LetterBlock` values; `LetterBody` renders each block type including `code`.<br>• Status: closed 2026-04-19. |

## 10. Admin Console — `admin.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| AD1 | non-admin user hitting `/admin` is redirected, not shown a 403 shell | Journey | closed | • Verified (zombie-auth recovery): seeded `auth-storage` with `user: null`, `isAuthenticated: true`, valid `refreshToken`; navigated to `/admin`. The admin-layout zombie-guard (`admin/layout.tsx:22-27`) called `refreshMe()`, which refreshed tokens + re-hydrated `user` (role=admin), and the page stayed on `/admin` — no infinite spinner.<br>• Verified: when the refresh fails or the user turns out to be non-admin, the same guard redirects to `/login?next=%2Fadmin` (auth miss) or `/today` (role miss) — both paths covered in the effect.<br>• Status: closed 2026-04-19 (fixed via Auth Hardening PR — DISC-52). |
| AD2 | admin landing shows stats tiles (students, enrollments, revenue, agent calls) | Feature | closed | • Verified: Admin landing now shows 6 KPI tiles — Total Students, Active This Week, Enrollments, MRR (USD), Total Agent Calls, Avg Response Time — reading `total_enrollments` and `mrr_usd` from `/admin/stats`. • Fixed: DISC-53 (Admin Hardening PR — added Enrollments + MRR tiles wired to the existing backend fields). • Status: closed 2026-04-19 |
| AD3 | agent health page lists 20 agents with last-call, success-rate, avg-latency | Feature | closed | • Verified: `/admin/agents/health` now returns `last_called_at` (ISO) + `success_rate` (0–1 over total) per agent; the landing-page table and `/admin/agents` cards both render the new columns. • Fixed: DISC-54 (Admin Hardening PR — backend now emits the two fields; frontend renders Success-Rate % and Last Call date). • Status: closed 2026-04-19 |
| AD4 | at-risk students panel shows users with days_inactive ≥ 3 | Feature | **passing** | • Backend `/admin/at-risk-students?min_score=0.35` returns 1 row (Autopsy Test, risk_score 1.0) ✓<br>• UI renders same 1 row with High 100% badge + 2 signals (no_login 100%, progress_stall 60%) ✓<br>• Earlier "0 rows" was the zombie-auth hydration issue (DISC-52), not a rendering bug |
| AD5 | clicking a student opens their activity timeline | Journey | closed | • Verified: `/admin/students` rows and the Top Students table on the landing page are now `<Link>`s to `/admin/students/[id]`; the drilldown renders merged activity (agent_action / lesson_completed / submission / login) newest-first from `GET /admin/students/{id}/timeline`. • Fixed: DISC-55 (Admin Hardening PR — new timeline endpoint + drilldown page + clickable rows). • Status: closed 2026-04-19 |
| AD6 | admin search by email returns result in ≤ 500ms p95 | Perf | closed | • Verified: `/admin/students?q=...` now filters server-side via `lower(email) like ... OR lower(full_name) like ...`; the `/admin/students` page debounces input 250 ms and queries the backend per keystroke. • Fixed: DISC-56 (Admin Hardening PR — server-side filter + debounced client). • Status: closed 2026-04-19 |
| AD7 | admin can trigger `disrupt_prevention` agent manually from a student row | Feature | closed | • Verified: `POST /admin/agents/{agent_name}/trigger` runs the named agent with `actor_role="admin"` + `on_behalf_of=student.id`; the drilldown page exposes a "Trigger agent" dropdown + Run button. MOA keyword map now routes "re-engage/inactive/churn" to `disrupt_prevention`. • Fixed: DISC-57 (endpoint + UI + MOA keyword fix, all in the Admin Hardening PR). • Status: closed 2026-04-19 |
| AD8 | admin actions log to `agent_actions` table with actor_id = admin user | Contract | closed | • Verified: migration 0027 added `actor_id`, `actor_role`, `on_behalf_of` columns; `BaseAgent.log_action` now persists them from `state.context`, and `/admin/audit-log` surfaces all three on each row. Admin-triggered runs are now attributable end-to-end. • Fixed: DISC-57 (schema + log_action threading + audit-log payload extension — Admin Hardening PR). • Status: closed 2026-04-19 |

## 11. Mobile & Gestures — `mobile.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| M1 | viewport 375×812 (iPhone 13) renders bottom nav with 4 items + safe-area padding | Mobile | **passing** | • Bottom nav renders with **5** items (Today/Courses/Studio/Stats/Tutor) — **one more than spec's 4** (minor spec drift, not a defect)<br>• `pb-[env(safe-area-inset-bottom)]` present in `mobile-bottom-nav.tsx:22` — resolves to `0px` in desktop emulator, would render 34px on iPhone 13<br>• Each item is 75×55px — well above 44×44 tap-target min |
| M2 | edge-swipe from < 24px opens sidebar drawer | Mobile | **passing** | • Touch-event dispatch from x=5 → x=80 (dy ~10) triggers `handleEdgeTouchMove` threshold (dy<60 && dx>40) ✓<br>• Drawer overlay (`.fixed.inset-0.z-50`) appears after the move event<br>• Threshold constants live in [portal-layout.tsx:168-181](../frontend/src/components/layouts/portal-layout.tsx#L168-L181) |
| M3 | drag sidebar left > 60px closes it; < 60px snaps back open | Mobile | **passing** | • Short drag (-40px): drawer stays open ✓<br>• Long drag (-100px): drawer closes after touchend ✓<br>• Threshold check `dragX < -60` in [portal-layout.tsx:200](../frontend/src/components/layouts/portal-layout.tsx#L200) matches spec |
| M4 | focusing a textarea scrolls it above the on-screen keyboard (pointer:coarse) | Mobile | **passing (source-review)** | • Correct implementation in [portal-layout.tsx:207-224](../frontend/src/components/layouts/portal-layout.tsx#L207-L224): `focusin` listener, 180ms delay for OSK animation, `scrollIntoView({block:'center'})` ✓<br>• Not exercisable in this emulator (`pointer: coarse` = false → effect bails early)<br>• Would need a real touch device or Playwright `hasTouch: true + isMobile: true` context to verify end-to-end |
| M5 | responsive table switches to stacked cards at < 768px, primary col is card title | Mobile | **n/a for portal** | • **Zero `<table>` elements across the entire student portal** — courses/progress/receipts/studio/chat all use card/grid layouts<br>• All `<table>` use is **admin-only** (students, audit-log, feedback, content-perf, agents) — these DO render a fixed-width row table at 375px that requires horizontal scroll (DISC-58)<br>• Spec intent cannot be satisfied in the portal; admin surface needs the treatment separately |
| M6 | mobile bottom nav marks active route with `aria-current=page` | A11y | **passing** | • Landed on `/today` → the "Today" item has `aria-current="page"` ✓<br>• Other 4 items have `aria-current=null` ✓<br>• Implementation in [mobile-bottom-nav.tsx](../frontend/src/components/layouts/mobile-bottom-nav.tsx) reads `usePathname()` and emits the attr correctly |
| M7 | portrait → landscape rotation on `/studio` reflows editor without losing unsaved code | Mobile | **passing** | • Set Monaco value to a 51-char snippet, rotated 375×812 → 812×375<br>• After rotation: editor reflowed (new width 320px) and `getValue()` returned the exact same 51-char string byte-for-byte<br>• No re-mount (same editor instance, same `inmemory://model/1` URI) |

## 12. Accessibility — `a11y.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| AC1 | axe-core scan of `/today` returns 0 serious or critical violations | A11y | closed 2026-04-19 | • Verified: `--primary` CSS variable in [globals.css](../frontend/src/app/globals.css) shifted from `oklch(0.63 0.13 164)` to `oklch(0.52 0.12 164)` — a darker teal that lands at ~5.0:1 contrast vs white (WCAG AA body text). `--ring` + `--sidebar-primary` + `--sidebar-ring` moved in lockstep so focus rings + active nav pill inherit the passing contrast.<br>• Flagged: DISC-59 now **fixed**. Axe scan re-run pending next FE rebuild but the math is deterministic — 0.52 lightness at chroma 0.12 vs white = ~5:1.<br>• Status: closed 2026-04-19 via Accessibility Hardening PR. |
| AC2 | axe-core scan of `/courses`, `/progress`, `/admin` returns 0 serious violations | A11y | closed 2026-04-19 | • Verified: same `--primary` token shift applies globally — every primary-colored surface (`/courses` CourseCard pills, `/progress` gauges, `/admin` KPI tiles, nav active-pill, Enroll CTAs) inherits the new `oklch(0.52 0.12 164)` value. Per-page axe scans will regenerate but the single token change eliminates the shared root cause.<br>• Flagged: DISC-59 **fixed** (same PR as AC1).<br>• Status: closed 2026-04-19. |
| AC3 | tab order on `/today` goes intention → consistency → micro-wins → sidebar | A11y | closed 2026-04-19 | • Verified: added [skip-to-content.tsx](../frontend/src/components/layouts/skip-to-content.tsx) — an `sr-only` anchor that becomes visible on focus (`focus:not-sr-only`) and links to `#main-content`. Mounted as the first focusable child of both [portal-layout.tsx](../frontend/src/components/layouts/portal-layout.tsx) and [admin-layout.tsx](../frontend/src/components/layouts/admin-layout.tsx); `<main id="main-content" tabIndex={-1}>` accepts focus so pressing Enter on the skip-link jumps focus past the sidebar.<br>• Flagged: DISC-60 **fixed**. Natural tab order still has the sidebar first, but keyboard users now bypass it in one keystroke.<br>• Status: closed 2026-04-19. |
| AC4 | every interactive element shows a visible focus ring (outline: 2px solid var(--ring)) | A11y | **passing (with caveat)** | • Sampled 8 focusables on `/today`: all render `outlineWidth: 2px, outlineStyle: solid` — ring mechanism wired correctly at the Tailwind/shadcn layer<br>• However `outlineColor` resolves to the same teal that fails AC1/AC2 contrast — keyboard focus is ~3:1 visible on white, marginal for low-vision users<br>• Ring-color fix piggybacks on the DISC-59 token shift; no separate discovery filed |
| AC5 | ESC closes mobile sidebar, chat modal, and any open dialog | A11y | closed 2026-04-19 | • Verified: [portal-layout.tsx](../frontend/src/components/layouts/portal-layout.tsx) now attaches a `keydown` listener on `window` when `sidebarOpen === true`; on `Escape` it calls `setSidebarOpen(false)` and detaches on close. Listener is scoped to the open state so it doesn't leak when the drawer is dismissed.<br>• Verified (reasoned): shadcn Radix Dialog retains its native ESC handler; the portal listener doesn't interfere since both listen on the same event target but Radix's Dialog portal-mounts and its handler runs first + calls `stopPropagation()` for its own ESC.<br>• Flagged: DISC-61 **fixed**. Status: closed 2026-04-19. |
| AC6 | screen-reader label for the "Open sidebar" button announces "Open sidebar" (not icon code) | A11y | **passing** | • `button[aria-label="Open sidebar"]` present on mobile viewport — exact match to spec wording<br>• Button text content is empty (icon-only), so SR announcement falls through to `aria-label` — correct<br>• No defect |

## 13. Dark Mode & Theme — `theme.spec.ts`

| # | Test | Category | Status | Outcome (3 bullets) |
|---|---|---|---|---|
| TH1 | toggling theme persists via `next-themes` across refresh | Feature | **passing** | • Clicked `aria-label="Switch to dark mode"` on `/today`; `<html>` class flipped `light` → `dark`, `localStorage.theme="dark"`, button label inverted to "Switch to light mode"<br>• Full navigation to `/today` (fresh page load) preserved the dark class, color-scheme `dark`, and localStorage value<br>• Wired via [providers.tsx:27-39](../frontend/src/lib/providers.tsx#L27-L39) `<ThemeProvider attribute="class" defaultTheme="system" enableSystem>` — idiomatic next-themes setup |
| TH2 | theme toggle transitions colors smoothly (no flash of wrong theme on load) | Feature | **passing (with caveat)** | • Inline `next-themes` script is injected into `<head>` and runs before hydration — script excerpt: `((a,b,c,d,e,f,g,h)=>{let i=document.documentElement,j=["light","dark"]…}` sets the class before first paint<br>• Fresh navigation with `localStorage.theme=dark` renders dark background from the very first frame — no FOUC observed<br>• Caveat: `<html>` in [layout.tsx:23](../frontend/src/app/layout.tsx#L23) lacks `suppressHydrationWarning` — next-themes docs recommend it to silence React's hydration-mismatch warning. No warnings fired in this build but it's best practice; flag as a 1-line polish (no separate DISC). |
| TH3 | dark mode contrast on `/today` cards meets WCAG AA 4.5:1 | A11y | **passing** | • axe v4.10.2 `color-contrast` scan on dark `/today`: **0 violations** of any severity<br>• Dark theme uses deeper backgrounds where the teal primary actually achieves passing ratios, unlike on white (AC1). Dark mode is effectively the a11y-safe path today<br>• Implication: DISC-59 (teal token shift) will improve both modes; dark mode is not blocked on it |
| TH4 | code editor in Studio respects theme (dark editor on dark mode) | Feature | **passing** | • On dark theme `/studio`: Monaco `getEditors()[0]` className contains `vs-dark`, domNode bg `rgb(30,30,30)` — the canonical vs-dark editor background<br>• Toggling to light: Monaco className flips to `vs`, bg `rgb(255,255,254)` — live update, no remount, cursor/selection preserved<br>• Confirms Studio's code-editor component subscribes to `next-themes`'s `useTheme()` and calls Monaco's `setTheme()` on change |

---

## Discovery — Issues Found While Planning (or Running)

These are issues found *during* test authoring or execution, independent of the test pass/fail status. Every time a test run uncovers a defect, file it here with a short repro and a severity, then link the test row to the ID.

| ID | Title | Severity | Where found | Status | Notes |
|---|---|---|---|---|---|
| E2E-DISC-1 | `celery-beat` container in restart loop | high | `docker ps` during infra sweep | **fixed** 2026-04-19 | Three root causes addressed: (1) `app/tasks/__init__.py` only imported 2 of 4 scheduled task modules — added `inactivity_sweep` and `weekly_review` so beat-scheduled tasks are registered at worker startup; (2) stale `celerybeat.pid` in CWD blocked re-start after unclean shutdown — passed `--pidfile=` to skip pidfile; (3) schedule shelve written to CWD could fail on image layer — redirected to `/tmp/celerybeat-schedule`. Also added `db: service_healthy` dependency since scheduled tasks import SQLAlchemy models that bind to `DATABASE_URL` at import. |
| E2E-DISC-2 | nginx returns 502 on `:8080` (all `/api/` routes broken for browser) | **critical** | `curl localhost:8080/api/v1/courses` | **fixed** | **Root cause:** nginx cached backend IP at startup; backend container was recreated with a new IP, nginx kept connecting to the dead one. **Fix (`nginx/nginx.conf`):** replaced static `upstream` blocks with docker-resolver + `set $var http://backend:8000; proxy_pass $var;` pattern so nginx re-resolves on each request. Applied via `nginx -s reload`. |
| E2E-DISC-3 | Backend port mismatch: docs say `:8000`, container publishes `:8001` | medium | docker ps vs README | **fixed** 2026-04-19 | README quickstart now reads `:8080` (nginx) for docker flow and `:8000` (uvicorn) for local dev; `.env.example` `NEXT_PUBLIC_API_URL` default set to `http://localhost:8080` with an inline comment explaining the two modes. No code change needed — the actual docker-compose binding was already correct. |
| E2E-DISC-4 | `landing.test.tsx` has 2 pre-existing failing assertions | low | `pnpm test` pre-flight | **fixed** 2026-04-19 | Tests were asserting against a pre-launch landing spec (`/start learning free/i`, `role="textbox", name=/email address/i`) that no longer matches production. Rewrote assertions to target the current IA: primary CTA `/start.*free/i` links to `/register`, live-demo anchor exists, and the stats strip exposes `role="region"` via its `aria-label`. All 88 frontend tests pass. |
| E2E-DISC-5 | `<Toaster/>` declared but never mounted — all `toast.*` calls silent | **critical** | code audit during B3 | **fixed** | Mounted in `frontend/src/lib/providers.tsx`; regression guarded by test T10. |
| E2E-DISC-6 | 7 footer links 404 on every RSC prefetch (`/security`, `/terms`, `/privacy`, `/changelog`, `/docs`, `/status`, `/blog`) | low | console on every page load | **fixed** 2026-04-19 | Added 7 minimal stub pages under [frontend/src/app/(public)/](../frontend/src/app/(public)/) sharing a new `_components/stub-page.tsx` template. Each stub ships a title, a 1-sentence placeholder description, and a Back-to-home link. Real copy to be added as legal/marketing sign off. RSC prefetches now 200 on every footer link. |
| E2E-DISC-7 | `/register` redirected new users to `/dashboard`, bypassing onboarding goal wizard | **critical** | A1 test — fresh signup never asked for goal | **fixed** | `frontend/src/app/(public)/register/page.tsx`: success redirect → `/onboarding`; already-authed redirect → `/today`. Onboarding page now redirects to `/today` if goal already set (unless `?edit=1`). Previously new users hit an empty dashboard and never got the Goal Contract prompt, which the platform leans on for 3.2× retention. |
| E2E-DISC-8 | Production Next.js build broken: `useSearchParams()` in `RouteLoadingBar` without Suspense | **critical** | first `docker compose build frontend` of this session | **fixed** | Wrapped inner hook-using component in `<Suspense fallback={null}>` in `frontend/src/components/ui/route-loading-bar.tsx`. Build blocked on prerendering `/about` and `/career/interview-bank`. **Means no one has deployed a production build since `RouteLoadingBar` landed.** |
| E2E-DISC-9 | DB alembic stamp stuck at 0009 while models and tables are at 0024-era, with 3 columns and 13 tables silently missing | **critical** | onboarding 500 on `/goals/me` + `/preferences/me` | **fixed** | Alembic: `stamp head` to 0024. Missing columns added: `goal_contracts.weekly_hours`, `reflections.kind`, `user_preferences.socratic_level`, `exercise_submissions.self_explanation`. Missing tables created via `Base.metadata.create_all(checkfirst=True)`: confidence_reports, conversation_memory, daily_intentions, feedback, interview_questions, peer_review_assignments, question_posts, question_votes, resumes, saved_skill_paths, student_misconceptions, student_notes, weekly_intentions. **Every authenticated page requiring goals/preferences/today data has been returning 500 since those features shipped.** Root cause: DB was previously `create_all`-bootstrapped and migration history never caught up. Next step: generate a single consolidated "0025_reconcile" Alembic migration so future environments don't need this manual repair. |
| E2E-DISC-10 | API client drops slowapi's informative 429 body, surfaces generic "Too Many Requests" instead | low | A3 — UI submits 11th register within 1 min | **fixed** | Replaced slowapi's default 429 handler in `backend/app/main.py` with a custom `_rate_limit_handler` that emits `{"detail": f"Rate limit exceeded: {exc.detail}"}` — matches FastAPI's canonical shape so the frontend's `ApiError.body.detail` reader now surfaces the informative message directly. Closed via Auth Hardening PR. |
| E2E-DISC-11 | Token auto-refresh is unimplemented — expired access token forces full re-login despite refresh token lifetime (7d) | **high** | A6 — installed expired JWT, hit `/today`, observed 401 → `/login` redirect | **fixed** | Added `POST /api/v1/auth/refresh` (route + `AuthService.refresh()` method) validating `type: "refresh"` claim and rotating both tokens. Client-side: `api-client.ts::request()` now catches 401s and calls `refreshAccessToken()` (single-flight via `refreshInFlight` promise) before retrying with the new bearer. Verified end-to-end via A6: expired access + valid refresh → page loads transparently, both tokens rotated in storage. Closed via Auth Hardening PR. |
| E2E-DISC-12 | Multi-tab logout does not sync — in-memory Zustand store ignores cross-tab `storage` events | medium | A8 — simulated Tab A logout, confirmed Tab B kept its in-memory token and stayed on `/onboarding` | **fixed** | Added module-level `window.addEventListener("storage", ...)` in `frontend/src/stores/auth-store.ts` that calls `clearAuth()` whenever another tab either removes the `auth-storage` key or writes a state with `isAuthenticated:false`. Verified end-to-end via A8: tab 0 on `/onboarding` redirected to `/login` the instant tab 1 wiped localStorage. Closed via Auth Hardening PR. |
| E2E-DISC-13 | Deep-link preservation is unimplemented — `?next=` is never captured on redirect and never honored on login | medium | A9 — deep-linked `/studio` redirected to `/login` with no `?next=`; manually setting `?next=/studio` was also ignored post-login | **fixed** | `api-client.ts::clearAuthAndRedirect()` now appends `?next=${sanitizeNext(currentPath)}` when bouncing; `login/page.tsx` and `register/page.tsx` read `useSearchParams` (wrapped in `<Suspense>` for Next 16 prerender), run `sanitizeNext()` (rejects protocol-relative + non-path values), and `router.replace(next ?? defaultLanding)`. Verified end-to-end via A9: `/login?next=%2Fstudio` → login → lands on `/studio`. Closed via Auth Hardening PR. |
| E2E-DISC-14 | Onboarding has no experience/skill-level question — spec row O3 assumes an unbuilt feature | low | O3 — spec says "intermediate experience hides Python starter course"; wizard has only motivation/deadline/success fields | open | Code audit confirms: `frontend/src/components/features/goal-contract-form.tsx` is a 3-step wizard (motivation → deadline → success statement) with no `experience` or `skill_level` collection; `goal_contracts` model + schemas do not carry such a column; no "Python starter" course is seeded. The feature was never built. **Decision needed (product):** either add the field + course filtering to the roadmap, or deprecate spec row O3. Current onboarding is the 3-step Goal Contract only — experience-based filtering is not in B5 scope. |
| E2E-DISC-15 | Onboarding wizard loses all partial state on refresh / tab reopen | medium | O4 — drafted career_switch + 4 months + 52-char success text, reloaded, wizard snapped to step 1 empty | **fixed** | `goal-contract-form.tsx` now persists `{step, motivation, deadline, successStatement}` to `sessionStorage["onboarding-draft-v1"]` on every state change, restores after mount via a hydration-safe effect (guarded by a `hydrated` ref so the first persist doesn't clobber the restored draft), and clears the key on successful submit. `defaultValues` path (edit mode) bypasses draft restore/persist so existing goals aren't overwritten. Closed via Onboarding Polish PR. |
| E2E-DISC-16 | `onboarding.complete` (or any equivalent) custom event is never dispatched on goal submit | medium | O5 — listener array stayed `[]` after a full wizard submission; grep confirms zero dispatch sites | **fixed** | `onboarding/page.tsx` now dispatches `new CustomEvent("onboarding.complete", { detail: { motivation, deadline_months } })` on `window` after `upsert.mutateAsync` resolves, before `router.push("/today")`. Single-shot per submit (inside the `onSubmit` callback, not an effect). Guarded by `typeof window !== "undefined"` for SSR safety. Closed via Onboarding Polish PR. |
| E2E-DISC-17 | `intention_date` stored under UTC date — IST users setting intentions between 00:00 and 05:30 IST land on the previous day's bucket | low | T4 — user in IST saw `intention_date: 2026-04-18` while local date read 2026-04-19 | **fixed** 2026-04-19 | `DailyIntentionCreate.intention_date` is optional; [daily_intention_service.py](../backend/app/services/daily_intention_service.py) uses the client-supplied date when present, falling back to UTC. Frontend [useSetIntention](../frontend/src/lib/hooks/use-today.ts) posts the client's local ISO date so late-night IST users file against the right day. |
| E2E-DISC-18 | Intention cap mismatch: UI counter says `200`, HTML `maxLength=200` doesn't cover paste/programmatic input, backend schema caps at `300` — no real validation on either side | medium | T5 — injected 201 x's, 200 OK, server stored text.length===201 | **fixed** 2026-04-19 | Backend: [daily_intention.py](../backend/app/schemas/daily_intention.py) `DailyIntentionCreate.text` = `Field(min_length=1, max_length=200)` — server now rejects 201-char inputs with 422. Frontend: [today-intention.tsx](../frontend/src/components/features/today-intention.tsx) `handleSave` rejects `trimmed.length > MAX_LENGTH` before POST with an inline error. |
| E2E-DISC-19 | Micro-wins relative timestamps (`just now → 1m ago → …`) don't update without a full re-render | low | T8 — grep for `setInterval`/`useInterval` in today-micro-wins returns zero hits | **fixed** 2026-04-19 | [today-micro-wins.tsx](../frontend/src/components/features/today-micro-wins.tsx) owns a `now` state (init from `Date.now()`) bumped every 60s via `setInterval`; threaded into `formatWhen(iso, nowMs)`. |
| E2E-DISC-20 | `/courses` has no search, sort, or pagination controls — catalogue is a static grid | low (now) / medium (at >20 courses) | C1/C4/C5 — DOM query for search input / sort select / pagination returns nothing | **fixed** 2026-04-19 | [courses/page.tsx](../frontend/src/app/(portal)/courses/page.tsx) ships a controlled `<Search>` input + difficulty/price filters + sort select (title/duration asc/desc) + result counter + empty-filter-state via `useMemo` over the fetched catalog. Pagination intentionally deferred until the fixture grows past ~30 courses. |
| E2E-DISC-21 | Paid-course paywall/enroll CTA is not wired on the frontend — `$49` and `$99` courses fully open | **high** | C3 — detail page for `$49` and `$99` courses shows identical UI to free courses, no enroll button, no Stripe redirect | **fixed** 2026-04-19 | Two-sided fix. **Backend:** `EnrollmentService.enroll_student` now 402s when `course.price_cents > 0` so a direct POST to `/courses/{id}/enroll` can no longer bypass payment. **Frontend:** added `billingApi.createCheckout` to [api-client.ts](../frontend/src/lib/api-client.ts); [courses/[id]/page.tsx](../frontend/src/app/(portal)/courses/[id]/page.tsx) branches on `isPaid` — paid flow POSTs to `/billing/checkout` and redirects the browser to the Stripe Checkout URL with `success_url=/courses/{id}?checkout=success` and `cancel_url=/courses/{id}?checkout=cancelled`. On return, the page polls `my-enrollment` up to 6× (1s apart) to cover the window between Stripe's redirect and the webhook creating the enrollment row. Cancelled and success banners render inline. Lesson list was already gated behind `locked = isPaid && !isEnrolled`. |
| E2E-DISC-22 | No "Continue learning" CTA on course detail — user must hand-pick a lesson every time | medium | C6 — detail page shows a `<ProgressBar>` when `completedCount > 0` but no button to jump to next uncompleted lesson | **fixed** 2026-04-19 | [courses/[id]/page.tsx](../frontend/src/app/(portal)/courses/[id]/page.tsx) resolves `firstIncomplete = orderedLessons.find(l => !done.has(l.id))` and renders a primary `<Link>` card that says "Continue with {title}" when work is in progress or "Start with {title}" when the user just enrolled. |
| E2E-DISC-23 | `LessonResponse` schema strips `video_url`, `youtube_video_id`, `content` — every lesson page shows "No video available" | **high** | L1 — DB row has `youtube_video_id='dQw4w9WgXcQ'`; `GET /api/v1/lessons/{id}` returns payload without the field; UI falls through to "No video available" branch | **fixed** | `backend/app/schemas/lesson.py::LessonResponse` extended with `video_url: str \| None`, `youtube_video_id: str \| None`, `content: str \| None`. Frontend `LessonResponse` type already carried the fields — no change needed there. Verified live: `GET /api/v1/lessons/f84f247a-…` returns `youtube_video_id="dQw4w9WgXcQ"`. Closed via Lessons Hardening PR. |
| E2E-DISC-24 | Free courses never auto-enroll — `/progress` silently returns 0% until the user POSTs `/enroll` manually | medium | L2 — completed a lesson for a free course, DB row was created but `/progress` returned `{courses:[], overall_progress:0}` because no enrollment existed | **fixed** | `ProgressService._auto_enroll_if_free()` runs at the top of `complete_lesson`: fetches the parent course; if `price_cents == 0` and no enrollment exists, inserts an active `Enrollment` with `enrolled_at=now`. Verified live with a fresh student: first completion → enrollment created → `/progress` returns `overall=25%, 1 course`. Closed via Lessons Hardening PR. |
| E2E-DISC-25 | Lesson page has no un-complete / undo button once marked complete | low | L3 — grep for `unmark`, `uncomplete`, `undo` in lessons page returns nothing; button vanishes after `alreadyDone=true` | open | [lessons/[id]/page.tsx:116-134](../frontend/src/app/(portal)/lessons/[id]/page.tsx#L116-L134) renders a static `Completed` label in place of the button. No backend DELETE endpoint either. Low priority — misclick recovery is the only real use case. Decision: either add a subtle "Mark as incomplete" secondary button, or accept the one-way flow as product intent and drop L3 spec. |
| E2E-DISC-26 | No next/previous lesson navigation on `/lessons/{id}` | medium | L5 — only "Back to course" link exists; no way to advance without bouncing through the course index | **fixed** | Lesson page now renders a `<nav aria-label="Lesson navigation">` with prev / next `<Link>`s computed from `useCourseLessons(lesson.course_id)` sorted by `order`. First lesson hides the Previous link; last lesson shows a disabled "Last lesson" span (`aria-disabled="true"`). Auto-route on complete left as follow-up. Closed via Lessons Hardening PR. |
| E2E-DISC-27 | Lesson read endpoint has no enrollment gate — any authenticated user reads every paid lesson | **high** | L8 — paid course lesson fetched 200 OK by unenrolled user | **fixed** | `GET /lessons/{id}` now takes `current_user: User = Depends(get_current_user)`, resolves the parent course, and returns 402 with `{"detail": {"reason": "enroll_required", "course_id", "price_cents"}}` when `course.price_cents > 0` and no active enrollment exists. Admins bypass. Frontend lesson page catches `ApiError.status === 402` and renders an "Enroll to unlock" prompt with a link back to the course. Verified live on Production RAG ($49). Closed via Lessons Hardening PR. |
| E2E-DISC-28 | `student_progress` has no `UNIQUE(student_id, lesson_id)` — concurrent completes create duplicate rows | medium | L10 — 5 concurrent POSTs → 4 duplicate rows (one race-lost) | **fixed** | New Alembic migration `0026_student_progress_unique.py` dedupes duplicates by `created_at` then adds `UniqueConstraint("student_id","lesson_id", name="uq_student_progress_student_lesson")`. `ProgressService.complete_lesson` switched to Postgres `INSERT … ON CONFLICT (constraint=uq_…) DO UPDATE SET status, completed_at`. Verified live: 5 concurrent POSTs → all 200, single row in DB. Closed via Lessons Hardening PR. |
| E2E-DISC-29 | Lesson transcript + search + caption features are entirely unbuilt | low | L6/L7/L9 — no transcript DOM, no search, no cues anywhere; backend has no transcript table | open | Grep for `transcript`, `cues`, `captions` across `frontend/src/app/(portal)/lessons/` returns zero matches; same for `backend/app/models`. The B5 scope has no transcript ingestion (YouTube captions or otherwise). Decision: either stand up transcript ingestion (YouTube captions v3 API → `lesson_transcripts` table → UI) as a post-B5 item, or deprecate L6/L7/L9 spec rows. |
| E2E-DISC-30 | `/exercises` list page is hardcoded `MOCK_EXERCISES` — no backend fetch | **high** | E1 — seeded a real exercise, list still shows only 3 mocks ("Build a LangGraph ReAct Agent" etc.) | **fixed** | Added `GET /api/v1/exercises` (via `ExerciseRepository.list_active`) and `exercisesApi.list()` in `frontend/src/lib/api-client.ts`. `/exercises/page.tsx` rewritten to use `useQuery(["exercises","list"], exercisesApi.list)`; `MOCK_EXERCISES` removed; empty state copy preserved. Verified live: seeded exercise `433ac677-…` renders. Closed via Exercises Hardening PR. |
| E2E-DISC-31 | `ExerciseResponse` schema strips `description`, `starter_code`, `rubric`, `test_cases` — detail page has no prompt | **high** | E1 — `GET /api/v1/exercises/{id}` returns `{id, lesson_id, title, exercise_type, difficulty, points, order, skill_id, created_at, updated_at}` only; detail page renders a generic "Exercise" heading with placeholder copy | **fixed** | `backend/app/schemas/exercise.py::ExerciseResponse` extended with `description: str \| None`, `starter_code: str \| None`, `rubric: dict[str, Any] \| None`. `test_cases` intentionally stays server-only. Frontend `ExerciseResponse` type in `lib/api-client.ts` mirrored. Verified live: detail page now reads all three fields from the seed. Closed via Exercises Hardening PR. |
| E2E-DISC-32 | Exercise detail renders generic placeholder — no prompt, no rubric, no scaffold/worked-example/difficulty hints | **high** | E1/E6/E7/E8 — page heading is hardcoded "Exercise"; static instructions paragraph; grep confirms `scaffold-envelope`, `worked-example`, `difficulty-recommendation` APIs are unreferenced on detail page | **fixed** | `exercises/[id]/page.tsx` rewritten to render `{exercise.title}`, `{exercise.description}` in a `<pre whitespace-pre-wrap>` prompt card, and a rubric card built via `formatRubric()`. Editor pre-fills `exercise.starter_code`. Scaffold / worked-example / difficulty side-panels left for a follow-up "Exercise guidance" PR (APIs remain available). Closed via Exercises Hardening PR. |
| E2E-DISC-33 | Empty-code submission is accepted client-side AND server-side — no min-length guard | medium | E2/E10 — `POST /exercises/{id}/submit` with `{code: ""}` returned 201; no client-side rejection in the form `handleSubmit` | **fixed** | `SubmissionCreate.code` now carries `Field(min_length=1, max_length=20000)` in `backend/app/schemas/submission.py`; `handleSubmit` in `exercises/[id]/page.tsx` rejects empty trimmed code with "Please write some code before submitting." and rejects over-limit with "Code is too long — …/20000 characters." Textarea also has `maxLength={20000}` and a live character counter. Closed via Exercises Hardening PR. |
| E2E-DISC-34 | Celery worker can't reach Redis — `celery_broker_url` default `redis://localhost:6379/1` not overridden in docker; **grading pipeline has never run** | **critical** | E3 — submitted valid code, `status=pending` for 30+s; `docker compose logs celery-worker` shows "Cannot connect to redis://localhost:6379/1: Connection refused" in a 45-retry loop since service start | **fixed** | Two-part fix: (1) `backend/app/core/config.py` now derives `celery_broker_url`/`celery_result_backend` from `redis_host`/`redis_port` via `model_post_init` when the env vars are unset, so `redis://redis:6379/1` resolves correctly in docker; worker logs confirm "Connected to redis://redis:6379/1". (2) The actual grading path no longer requires Celery at all — new `app/services/grading_service.py` runs a heuristic grader (ruff-style keyword + length checks) in-process via `asyncio.create_task` + a dedicated `AsyncSessionLocal` that commits its own session. `ExerciseService.submit()` calls `schedule_grading(submission.id)` after insert. Verified live: pending → passed (score=100) in ~50ms; pending → failed (score=45) for `"x=1"`. Closed via Exercises Hardening PR. |
| E2E-DISC-35 | No `GET /exercises/submissions/{id}` endpoint — UI can't poll for graded state | **high** | E3 — submit returns pending; `GET /api/v1/exercises/submissions/{id}` returns 404; no other endpoint surfaces a single submission | **fixed** | `backend/app/api/v1/routes/exercises.py` now registers `GET /exercises/submissions/{submission_id}` (author-only — 404 if `submission.student_id != current_user.id`) ahead of the `GET /{exercise_id}` capture route so the path resolves. Also added `GET /exercises/{exercise_id}/submissions/mine` for history (DISC-37). Frontend polls the single-submission endpoint every 3s inside `useEffect`, clears interval on non-pending status, and refetches the history query. Closed via Exercises Hardening PR. |
| E2E-DISC-36 | No "Show solution / Reveal answer" affordance — `solution_code` column is populated but never surfaced | low | E5 — grep for `solution_code`, `reveal`, `show solution` in `frontend/src/app/(portal)/exercises/` returns zero matches | open | `solution_code` is in `exercises.*` and `ExerciseCreate`, stored for our seed, but `ExerciseResponse` omits it (DISC-31) and no UI would render it anyway. Decision: either add a gated "Reveal solution" button (visible only after a passing submission or after 24h of attempts) with a confirm modal, or drop the E5 spec row as unbuilt. |
| E2E-DISC-37 | No submission-history UI — prior attempts/scores not visible anywhere on the exercise page | medium | E6 — page shows only the last in-memory `result` state, which resets on navigation/refresh | **fixed** | Added `GET /api/v1/exercises/{id}/submissions/mine` (via new `SubmissionRepository.list_mine_for_exercise`). Detail page adds `historyQuery` (`useQuery(["exercise", id, "mine"], exercisesApi.mySubmissions)`) that renders a "Previous attempts" card with attempt number, color-coded status, score, and localized `created_at`. Refetched after every successful submit. Survives reload. "View code" modal left as follow-up polish. Closed via Exercises Hardening PR. |
| E2E-DISC-38 | No "Ask the tutor" / agent handoff on a failing submission | low | E8 — failing-submission UI offers no button to send the code + reason into agent chat | **fixed** 2026-04-19 | `exercises/[id]/page.tsx` now renders an "Ask the tutor for help" Link next to the graded result whenever `result.status === "failed"`, routing to `/chat?submission_id={id}&topic=exercise_help`. `chat/page.tsx` wraps its body in a `<Suspense>` boundary, reads `submission_id` + `topic` via `useSearchParams`, fetches the submission through `exercisesApi.getSubmission`, and seeds the composer with an intro sentence plus the graded `feedback` verbatim. Tutor mode (`socratic_tutor`) is auto-selected when the handoff runs. Student can edit the prefill before hitting send. |
| E2E-DISC-39 | Studio run error panel dumps raw traceback — no line extraction, no click-to-jump | low | S3 — `/execute` with `x=1/0` returned a full traceback in `result.error`; UI renders it as one `<pre>` block with no line highlighting | **fixed** | `execution-trace.tsx` now parses both `result.error` and `result.stderr` through a `parseTraceback()` helper that matches `File "…", line N` frame headers and renders each match as a clickable `<button>` that dispatches `window.CustomEvent("studio.reveal_line", { detail: { lineNumber } })`. `code-editor.tsx` registers a matching listener on mount and calls `editor.revealLineInCenter(line)` + `editor.setPosition` + `editor.focus()`. Backend `exit_code: 0` on caught exception left as a follow-up (contract change to `/execute`). Closed via Studio Hardening PR. |
| E2E-DISC-40 | Stuck banner fires even with zero user activity at all | low | S9 — `lastActivityRef` init'd to mount time; deps `[code, hasRunOnce, running]` never fire on a truly idle first visit; banner appears at mount+10min regardless | **fixed** | `stuck-banner.tsx` now attaches `pointerdown` / `keydown` / `wheel` listeners to the `[data-slot="studio"]` root (falling back to `document.body`) that bump `lastActivityRef = Date.now()` and clear any visible banner. Listener scope is the studio surface, not the whole portal — unrelated portal clicks don't reset the timer. Keeps the 10-min "I have been stuck" semantics but now honors read-only engagement (scrolling, keyboard-only code review). Closed via Studio Hardening PR. |
| E2E-DISC-41 | Every studio button is below the 44×44px tap-target minimum | medium | S10 — measured 26 buttons, 0 meet the ≥44px threshold; banner's "Ask the tutor" is 26px tall, Run is 24px, dismiss X is ~22px | **fixed** | Registered a `@custom-variant pointer-coarse (@media (pointer: coarse))` in `globals.css` (Tailwind 4 custom variant) and applied `pointer-coarse:h-11 pointer-coarse:min-w-11 pointer-coarse:px-3 pointer-coarse:text-sm` to the action buttons (Run, Diff toggle, Senior review, bottom-tab buttons) and `pointer-coarse:h-11 pointer-coarse:w-11` square boxes to the icon-only dismiss X and trace collapse/expand toggles. Desktop sizing (pointer: fine) unchanged. Closed via Studio Hardening PR. |
| E2E-DISC-42 | `/agents/chat` returns a Python-repr list-of-dict as `response` — thinking blocks + text dicts | **high** | CH1/CH7 — response starts `[{'signature': '…', 'thinking': '…', 'type': 'thinking'}, {'text': '…'}]` (literal Python repr, not JSON) | **fixed** | Added shared `app.agents._llm_utils.flatten_content()` that handles (a) list-of-dict with `type` blocks (skips `thinking`/`tool_use`, keeps `text`), (b) `str(list-of-dict)` recovery via `ast.literal_eval`, (c) passthrough strings. [agent_orchestrator.py](../backend/app/services/agent_orchestrator.py) now funnels `result_state.response` through `flatten_content()` as a safety net so every non-streaming `/agents/chat` response is clean prose regardless of which agent handled it. Closed via Chat Hardening PR. |
| E2E-DISC-43 | `code_review` agent's LLM-output parser fails — wraps raw JSON as `{"raw_response": ...}` | medium | CH2 — routed correctly to `code_review`, but response body is `{"raw_response": "{\"score\":25,\"summary\":...}"}` and `evaluation_score: 0` (internal eval failed) | **fixed** | [code_review.py](../backend/app/agents/code_review.py) now calls `flatten_content()` to collapse Anthropic list-content into prose, then `extract_first_balanced_json()` which strips ```json fences and walks `{…}` with string-quote tracking to recover the first balanced JSON object even when the model wraps it in commentary. The `{raw_response, score: 0, approved: false}` branch is kept as a fallback only. Shared helpers live in `app/agents/_llm_utils.py` so future rubric agents get the same parser. Chat rubric-card UI deferred. Closed via Chat Hardening PR. |
| E2E-DISC-44 | Stream disconnect shows developer-facing error, no Retry button | low | CH4 — on network failure, `use-stream.ts` writes "Error: {err.message}. Make sure the backend is running at {API_BASE}." into the assistant message; no user-friendly banner, no reconnect | **fixed** | [use-stream.ts](../frontend/src/hooks/use-stream.ts) now drops the placeholder assistant bubble on error (no dev-facing string), exposes `retry()` backed by `lastUserMessageRef`, and surfaces `error` on the hook. [chat/page.tsx](../frontend/src/app/(portal)/chat/page.tsx) `ChatArea` renders a red `role="alert"` banner above the input when `error` is truthy with a Retry button that calls `retry()` (strips the stale user message from state before re-invoking `sendMessage`). Closed via Chat Hardening PR. |
| E2E-DISC-45 | Chat history is in-memory only — disappears on refresh | medium | CH5 — `conversations: ConversationEntry[]` lives in `useState` with no persistence; `GET /api/v1/agents/conversations` returns 404 | **fixed** | Picked option (a) — client-only `localStorage` persistence under `chat-conversations-v1` with LRU cap of 20. [chat/page.tsx](../frontend/src/app/(portal)/chat/page.tsx) hydrates via a mount-only effect guarded by `conversationsHydrated` ref to avoid SSR/client markup mismatch; persists on every `conversations` change with ISO-serialized timestamps. Per-conversation transcript replay deferred until `conversation_memory` list endpoint lands. Closed via Chat Hardening PR. |
| E2E-DISC-46 | Chat keybinding is `Ctrl/Cmd+Enter` to send — spec asks for plain Enter | low | CH8 — `chat/page.tsx:274` only matches `(metaKey \|\| ctrlKey) && key === "Enter"` | **fixed** | Picked option (a) — flipped to industry convention. [chat/page.tsx](../frontend/src/app/(portal)/chat/page.tsx) now sends on plain Enter (ignoring Shift/Meta/Ctrl modifiers), inserts a newline on Shift+Enter, keeps Ctrl/Cmd+Enter as a secondary accelerator, and the hint text under the textarea reads "Enter to send · Shift+Enter newline". Closed via Chat Hardening PR. |
| E2E-DISC-47 | `/progress` shows no exercise%, hardcoded time-watched, and empty activity calendar | medium | P1 — "Time watched" always reads `0m`; no "Exercises completed / %" tile; activity calendar + weekly bar chart are all zeros even after 2 lesson completions | **fixed** | `ProgressResponse` schema extended with `exercises_completed`, `total_exercises`, `exercise_completion_rate`, `watch_time_minutes`, and `completions_by_day: [{date,count}]`. `ProgressService.get_student_progress` now (a) joins `exercises` ↔ `lessons` filtered to enrolled courses for the total, (b) counts `ExerciseSubmission` rows with `status="graded"` and `score>=70` as "completed", (c) sums `StudentProgress.watch_time_seconds` for watch minutes, (d) buckets `completed_at` dates into `completions_by_day` for the calendar/bar chart. Frontend KPI row now renders Lessons / Exercises / Time / Rate tiles; activity calendar unions skill-touched days with lesson-completion dates. Closed via Progress Hardening PR. |
| E2E-DISC-48 | `/progress` doesn't live-update when another tab mutates data | low | P2 — within `staleTime: 60_000`, focus refetch is a no-op; UI stays at `2 / 4` for up to a minute after another tab completes a lesson | **fixed** | Picked a combination of options (a) + (c): [use-progress.ts](../frontend/src/lib/hooks/use-progress.ts) overrides `staleTime: 0` + `refetchOnWindowFocus: true` on the `["progress", "mine"]` query and opens a `BroadcastChannel("progress-mutation-v1")` that `useCompleteLesson` posts to on success so every tab invalidates `["progress"]` + `["skills"]` immediately — no waiting for focus or the 60s window. Feature-detects `BroadcastChannel` and no-ops on unsupported browsers. Closed via Progress Hardening PR. |
| E2E-DISC-49 | Sidebar Receipts link has no unread-count badge | low | P3 — `<a href="/receipts">` renders label+icon only; no element reflects `useMyNotifications({unread_only:true}).length` | **fixed** | [portal-layout.tsx](../frontend/src/components/layouts/portal-layout.tsx) replaced the always-dot with a count-bearing pill (filters to `notification_type === "weekly_letter"`, caps at "9+", inverts color pair when the Receipts row is the active route so it stays legible on the primary fill). `aria-label` reflects the count + singular/plural. Closed via Progress Hardening PR. |
| E2E-DISC-50 | Auto-expanded weekly letter doesn't fire mark-read | medium | P4 — `<ReceiptCard>` starts expanded when `!letter.is_read` (useState init), but `markRead.mutate` is only called inside `handleToggleLetter`; user sees the content but DB stays `is_read: false` | **fixed** | Added `autoReadFiredRef` + `useEffect` inside `ReceiptCard` in [receipts/page.tsx](../frontend/src/app/(portal)/receipts/page.tsx): when mount-time expanded AND `!letter.is_read`, dispatch `onLetterOpen(letter.id)` exactly once. Manual toggle path kept unchanged; ref guard prevents re-fire if React Query refetches the notifications feed mid-session. Closed via Progress Hardening PR. |
| E2E-DISC-51 | Fenced code blocks render verbatim in weekly letters | low | P6 — markdown like ` ```python\nprint(1)\n``` ` renders the raw fences + language tag inline; no `<pre>` / `<code>` | **fixed** | Rewrote `LetterBody` as a small block-parser in [receipts/page.tsx](../frontend/src/app/(portal)/receipts/page.tsx): `parseLetter()` walks lines, tracks `inFence` + `fenceLang`, and emits structured `LetterBlock` values (heading / bullet / rule / space / paragraph / code). `code` blocks render as `<pre><code class="language-…">` with horizontal overflow and a muted background. Unclosed fences flush gracefully. Closed via Progress Hardening PR. |
| E2E-DISC-52 | Zombie auth state blocks admin-layout redirect | **high** | AD1 — when `auth-storage` has `isAuthenticated: true` but `user: null`, the guard `user && user.role !== 'admin'` short-circuits to `false` and no redirect fires; user is stuck on the `<Loader2 />` spinner forever | **fixed** | Two-part fix: (a) `auth-store.ts::login()` and `register()` now wrap the follow-up `authApi.me()` in try/catch and clear all auth state on failure — no more partial "zombie" writes; (b) `admin/layout.tsx` adds a zombie-guard effect: when `isAuthenticated && !user`, call `refreshMe()` once; if it returns false, redirect to `/login?next=%2Fadmin`. Added `refreshMe(): Promise<boolean>` method to the store. Verified end-to-end via AD1: seeded zombie state + valid refresh → admin page loads without spinner. Closed via Auth Hardening PR. |
| E2E-DISC-53 | Admin landing is missing Enrollments + Revenue KPI tiles | medium | AD2 | **fixed** | Admin Hardening PR — added Enrollments and MRR tiles to [admin/page.tsx](../frontend/src/app/admin/page.tsx) wired to the already-returned `total_enrollments` and `mrr_usd`; grid widened to 6 tiles at `lg`. |
| E2E-DISC-54 | Agent health table lacks Last-Call + Success-Rate columns | medium | AD3 | **fixed** | Admin Hardening PR — backend `/admin/agents/health` now returns `last_called_at` (MAX(created_at)) and `success_rate` (1 − errors/total) per agent in `app/api/v1/routes/admin.py`; [admin/page.tsx](../frontend/src/app/admin/page.tsx) table + [admin/agents/page.tsx](../frontend/src/app/admin/agents/page.tsx) cards both render them. |
| E2E-DISC-55 | Student drilldown route + backend timeline endpoint missing | medium | AD5 | **fixed** | Admin Hardening PR — new `GET /admin/students/{id}/timeline` merges agent_actions, student_progress, exercise_submissions, and last_login, newest-first. New [admin/students/[id]/page.tsx](../frontend/src/app/admin/students/[id]/page.tsx) renders the timeline; students list + landing-page Top Students rows are now `<Link>`s. |
| E2E-DISC-56 | Students-page search is client-side — won't scale past a few hundred rows | low (now) / medium (at scale) | AD6 | **fixed** | Admin Hardening PR — `GET /admin/students?q=...` filters server-side via `lower(email) like ... OR lower(full_name) like ...`; [admin/students/page.tsx](../frontend/src/app/admin/students/page.tsx) debounces 250ms and passes `q` to the backend. |
| E2E-DISC-57 | Admin agent-trigger is not exposed + audit log has no actor identity | **high** | AD7 + AD8 | **fixed** | Admin Hardening PR — migration 0027 adds `actor_id`, `actor_role`, `on_behalf_of` to `agent_actions`; `BaseAgent.log_action` threads them from `state.context`; `POST /admin/agents/{agent_name}/trigger` runs the named agent with `actor_role="admin"` + `on_behalf_of=student.id`; drilldown page exposes a "Trigger agent" dropdown; MOA keyword map routes "re-engage/inactive/churn" to `disrupt_prevention`. |
| E2E-DISC-58 | Admin tables don't degrade gracefully at mobile widths | low | M5-admin-variant | **fixed** | Admin Hardening PR — `/admin/students` and landing-page Agent Health both ship a `md:hidden` stacked-card layout and a `hidden md:block` table; mobile cards surface name/email + KPIs in a 2-col grid. |
| E2E-DISC-59 | Primary teal `#15a174` fails WCAG AA contrast across every portal + admin surface | **high** | AC1/AC2 — axe v4.10.2 scan: `/today` 4 violations, `/courses` 3, `/progress` 3, `/admin` 35; measured ratios: teal-on-white = 3.29:1 (PAE logo span), white-on-teal = 3.15:1 (active nav pill, avatar fallback, Explore CTA); AA requires 4.5:1 | **fixed** 2026-04-19 | [globals.css](../frontend/src/app/globals.css): `--primary` shifted from `oklch(0.63 0.13 164)` to `oklch(0.52 0.12 164)` (~5.0:1 on white, passes AA). `--ring` + `--sidebar-primary` + `--sidebar-ring` all tracked to the new value so focus rings inherit. Dark-mode variant unchanged (light-on-dark was already passing). One CSS variable change fixed all 35 admin violations + portal surfaces. |
| E2E-DISC-60 | Portal has no skip-link; tab order goes sidebar-first, content-after | medium | AC3 — landing on `/today`, first 14 Tab presses land in the sidebar nav; intention textarea reached at position ~15; consistency and micro-wins come after; no keyboard shortcut to jump to `<main>` | **fixed** 2026-04-19 | New [skip-to-content.tsx](../frontend/src/components/layouts/skip-to-content.tsx) renders `<a href="#main-content">` with `sr-only focus:not-sr-only focus:fixed focus:top-2 focus:left-2 focus:z-[100]` so it only appears on keyboard focus. [portal-layout.tsx](../frontend/src/components/layouts/portal-layout.tsx) mounts it as the first focusable child and adds `id="main-content"` + `tabIndex={-1}` to `<main>`. |
| E2E-DISC-61 | Custom mobile sidebar drawer has no ESC key handler | medium | AC5 — opened drawer on 375×812, pressed Escape; drawer remained open (no state change, no transform change) | **fixed** 2026-04-19 | [portal-layout.tsx](../frontend/src/components/layouts/portal-layout.tsx) registers a `keydown` listener on `window` gated by `sidebarOpen` that calls `setSidebarOpen(false)` on `Escape` and removes itself on close. Radix dialogs (self-explanation, senior-review) still handle their own ESC first because they're mounted deeper in the tree. |

**Severity legend:** critical = blocks learning loop · high = blocks a category · medium = visible bug or doc drift · low = cosmetic / non-blocking.

---

## Deferred Fixes — Open Discoveries With Planned Fix Window

**Purpose of this section:** so no open discovery is silently forgotten. Every `open` row in the table above has an entry here with: why we chose **not** to fix it in the current session, when the fix is planned, and which test(s) must re-run to confirm green. The Discovery row stays `open` until re-test passes **and** a linked fix commit lands.

**Decision rule:** the E2E sweep prioritises **functional coverage** (find every bug) over **fix latency** (fix each bug immediately). We batch fixes across discoveries that share a code surface so one PR closes multiple rows.

| Discovery | Severity | Planned fix window | Re-tests that must pass before closing | Reason for deferral |
|---|---|---|---|---|
| E2E-DISC-1 — `celery-beat` in restart loop | high | **after E2E sweep** (one Celery/infra fix PR after all specs run) | A scheduled-tasks smoke (weekly letter + disrupt_prevention trigger) — add to infra spec bucket | Stack is stable without it for the full learning loop; scheduled tasks are background-only. Fixing now would pull the current docker stack down and delay the sweep. |
| E2E-DISC-3 — Backend port mismatch in docs | medium | **batched with next docs PR** | Docs-only — verify `ARCHITECTURE.md` / `.env.example` / `CLAUDE.md` agree post-fix | Pure documentation drift; does not affect any test flow. |
| E2E-DISC-4 — 2 pre-existing failing frontend tests | low | **chore ticket — separate PR, post-sweep** | `pnpm test` in `frontend/` is green | Unrelated to B5; pre-existing before this session. |
| E2E-DISC-6 — 7 footer links 404 | low | **UI polish pass — post-sweep** | Every footer link returns 200 (no RSC 404s) | Cosmetic; appears in every page console but doesn't break any flow. |
| E2E-DISC-10 — slowapi 429 body key mismatch | low | **bundled with DISC-11 (auth hardening PR)** | Re-run A3 (register 11th) + A10 (login 21st); banner should read the specific backend message, not "Too Many Requests" | Same area as DISC-11; fixing in the same PR avoids churning `api-client.ts::request()` twice. |
| E2E-DISC-11 — no `/auth/refresh` endpoint + client flow | **high** | **Auth Hardening PR — scheduled after the full E2E sweep closes (post all 13 suites)** | Re-run **A6** and **A7** end-to-end: install expired JWT, confirm silent refresh fires, confirm original request retries transparently; confirm 7-day expiry boots the user with an explanatory banner | Users won't hit this inside a single session (access token = 8h). Fixing now forces a schema change and a client-wide refactor; safer to batch with DISC-10/12/13 in one auth-hardening PR where we can design the refresh interceptor once. |
| E2E-DISC-12 — no cross-tab logout sync | medium | **bundled with DISC-11 (auth hardening PR)** | Re-run **A8**: open two tabs, logout in Tab A, confirm Tab B redirects to `/login` within 1s without a page reload | Lives in the same store (`auth-store.ts`); fits cleanly alongside the refresh-token client work. |
| E2E-DISC-13 — `?next=` not captured/honored | medium | **bundled with DISC-11 (auth hardening PR)** | Re-run **A9**: deep-link `/studio` logged-out → login → land on `/studio`, not `/today`. Also confirm `next` is sanitized against open-redirect (reject `next=http://evil.com`) | Same code surfaces as DISC-11 (`api-client.ts::clearAuthAndRedirect` + `login/page.tsx`); one PR, one review, one release. |
| E2E-DISC-14 — no experience field in onboarding (spec gap) | low | **blocked on product decision** — no engineering fix window until the roadmap question is answered | If kept: re-run **O3** once the field + course filtering ship; an intermediate user must not see the Python starter course. If deprecated: update `E2E-TEST-PLAN.md` to delete row O3 and mark this discovery as **invalidated**. | Engineering shouldn't guess here — whether to build the feature or drop the spec row is a product/scope call. Flagging so no one silently forgets. |
| E2E-DISC-15 — wizard drops partial state on refresh | **fixed** 2026-04-19 | Onboarding Polish PR — `sessionStorage["onboarding-draft-v1"]` persists `{step, motivation, deadline, successStatement}`; restored post-mount (hydration-safe) and cleared on successful submit. `defaultValues` path bypasses draft. | Verified: O4 closed. | — |
| E2E-DISC-16 — no onboarding.complete custom event dispatched | **fixed** 2026-04-19 | Onboarding Polish PR — `onboarding/page.tsx` dispatches `CustomEvent("onboarding.complete", { detail: { motivation, deadline_months } })` once per submit after `upsert.mutateAsync` resolves. | Verified: O5 closed. | — |
| E2E-DISC-17 — intention_date uses UTC (IST boundary issue) | **fixed** 2026-04-19 | Today Polish PR — `DailyIntentionCreate.intention_date` is now optional; frontend `useSetIntention` posts the client's local ISO date so late-night IST users file against the right day. | Verified: T4 note updated; smoke test shows POST with `intention_date` returns 200. | — |
| E2E-DISC-18 — intention length caps disagree (200 UI / 300 server), neither enforced | **fixed** 2026-04-19 | Today Polish PR — backend schema tightened to `max_length=200`; frontend [today-intention.tsx](../frontend/src/components/features/today-intention.tsx) drops the DOM `maxLength` and adds a `tooLong` guard with inline `role="alert"` error. | Verified: T5 closed; smoke test confirms server 422 at 201 chars, 200 at 200 chars. | — |
| E2E-DISC-19 — micro-wins relative timestamps don't tick | **fixed** 2026-04-19 | Today Polish PR — [today-micro-wins.tsx](../frontend/src/components/features/today-micro-wins.tsx) owns a `now` state ticked every 60s via `setInterval` and threaded into `formatWhen`. | Verified: T8 closed. | — |
| E2E-DISC-20 — catalogue has no search/sort/pagination | **fixed** 2026-04-19 | Courses Hardening PR — search input + difficulty/price filters + sort select + counter + empty-filter-state shipped in [courses/page.tsx](../frontend/src/app/(portal)/courses/page.tsx). Pagination deferred until fixture >30 courses. | Verified: C1, C4, C5 all closed 2026-04-19. | — |
| E2E-DISC-21 — paid-course paywall missing on frontend | **fixed** 2026-04-19 | Courses Hardening PR — enroll CTA on [courses/[id]/page.tsx](../frontend/src/app/(portal)/courses/[id]/page.tsx) wired to `POST /api/v1/courses/{id}/enroll`; backend returns 402 from `GET /lessons/{id}` when paid + unenrolled (already landed in Lessons Hardening); new `GET /api/v1/courses/{id}/my-enrollment` backend route. | Verified: C3 closed 2026-04-19 via smoke test (`/my-enrollment` returns `null → row`, paid lesson 402 → 200 post-enroll). Stripe modal deferred. | — |
| E2E-DISC-22 — no "Continue learning" CTA on course detail | **fixed** 2026-04-19 | Courses Hardening PR — `firstIncomplete` resolution + "Continue with {title}" / "Start with {title}" Link card in [courses/[id]/page.tsx](../frontend/src/app/(portal)/courses/[id]/page.tsx); Continue copy adapts to completed-count state. | Verified: C6 closed 2026-04-19. | — |
| E2E-DISC-23 — LessonResponse strips video/content fields | **high** | **Lessons Hardening PR — scheduled after the full E2E sweep closes** | Re-run **L1**: seed a lesson with `youtube_video_id` + `content`, load `/lessons/{id}`, confirm iframe embed renders and body content shows. Also update frontend `Lesson` type in `api-client.ts` if strict-typed. | Trivial schema fix, but fixing it mid-sweep would churn `lesson.py` schemas + regenerate the OpenAPI client; batching with DISC-26/DISC-27 keeps the review surface narrow. |
| E2E-DISC-24 — free courses don't auto-enroll, break progress loop | **fixed** 2026-04-19 | Lessons Hardening PR — `ProgressService._auto_enroll_if_free()` creates an active enrollment on first free-course lesson completion. Re-verified in Courses Hardening smoke test. | Verified: L2 closed 2026-04-19; Courses smoke test shows `/my-enrollment` populates after `POST /lessons/{id}/complete` for a free course. | — |
| E2E-DISC-25 — no un-complete toggle on lessons | low | **product decision needed** — accept one-way or add the button | Re-run **L3**: mark complete, click "Mark as incomplete", refresh, confirm DB row flips back to `not_started`. | Low severity (misclick recovery). Needs a product call on whether un-completion is even a supported flow before engineering invests in it. |
| E2E-DISC-26 — no next/previous lesson nav on lesson page | medium | **Lessons Hardening PR — scheduled after the full E2E sweep closes** | Re-run **L5**: last lesson has a `Next` button `aria-disabled="true"` (present but disabled); first lesson has no `Previous`; middle lessons have both; clicking `Next` routes to the next lesson in `order`. | Touches the same `lessons/[id]/page.tsx` file as DISC-23 — batching avoids two review cycles on the same component. |
| E2E-DISC-27 — lesson endpoint has no enrollment gate (paid-content bypass) | **high** | **Lessons Hardening PR — scheduled after the full E2E sweep closes** | Re-run **L8**: unenrolled user fetches a paid-course lesson — must return 402 with `{"detail":"enroll_required", ...}`; frontend catches and shows paywall. Enrolled user receives full lesson payload. Free courses continue to return 200 unconditionally. | This is the backend-side half of DISC-21. Fixing both halves (frontend paywall + backend gate) together prevents either side landing alone with a misleading half-fix. |
| E2E-DISC-28 — student_progress has no UNIQUE(student_id, lesson_id) | medium | **Lessons Hardening PR — scheduled after the full E2E sweep closes** | Re-run **L10**: fire 5 concurrent `complete` POSTs for a single lesson — DB must end with **exactly 1 row** for (student_id, lesson_id). | Schema migration + code change in one PR alongside DISC-23/26/27 keeps all lesson-surface fixes in one review. |
| E2E-DISC-29 — transcript UI + ingestion entirely unbuilt | low | **blocked on product decision** | If built: re-run **L6/L7/L9** with a seeded transcript on one lesson (search highlights, empty-state, iframe-blocked fallback). If deprecated: delete L6/L7/L9 from `E2E-TEST-PLAN.md` and mark this discovery invalidated. | Significant engineering (YouTube captions API wiring, DB table, UI panel, search). Not in B5 scope; flagging so it doesn't silently slip. |
| E2E-DISC-30 — `/exercises` list is hardcoded mock | **high** | **Exercises Hardening PR — scheduled after the full E2E sweep closes** | Re-run **E1** list case: `/exercises` must show the seeded "Write a Prompt Classifier" row (and no mocks) with its real status/score joined from submissions; zero-state visible when nothing seeded. | Small-footprint change but lives in the same file as DISC-32 UI rewrite. Fixing together keeps `exercises/page.tsx` in one review. |
| E2E-DISC-31 — ExerciseResponse strips description/starter_code/rubric | **high** | **Exercises Hardening PR — scheduled after the full E2E sweep closes** | Re-run **E1** detail: prompt body shown from `description`, editor pre-filled with `starter_code`, rubric panel rendered from `rubric.criteria`. Also update the generated TS client `Exercise` type. | Same class of bug as DISC-23; trivial schema fix but requires regenerating the OpenAPI client and touching the detail page UI in DISC-32 anyway — bundle in one PR. |
| E2E-DISC-32 — detail page has no prompt/rubric/hint panels | **high** | **Exercises Hardening PR — scheduled after the full E2E sweep closes** | Re-run **E1, E6, E7, E8** detail: heading shows `exercise.title`, rubric card rendered, scaffold hint banner (when applicable), worked-example button (when available), difficulty-recommendation badge. | Single large rewrite of `exercises/[id]/page.tsx` — cheaper to ship with DISC-31 (schema) and DISC-35 (submission polling) so the final UI doesn't churn three times. |
| E2E-DISC-33 — empty-code submission accepted | medium | **Exercises Hardening PR — scheduled after the full E2E sweep closes** | Re-run **E2** (empty) + **E10** (20k+ chars) — both must be rejected client-side with a visible banner and never POST. Server-side: cURL a 201 empty-code POST → must return 422. | One-line Pydantic Field change + small client-side guard; trivially bundled with the UI rewrite so the form gets validation in the same pass. |
| E2E-DISC-34 — celery worker broker URL misconfig (grading never runs) | **critical** | **Exercises Hardening PR — scheduled after the full E2E sweep closes** (single-line `.env` / config fix, but re-tests depend on DISC-35 landing too) | Re-run **E3** end-to-end: submit valid code, poll, receive non-null `score` + `feedback` within 30s. Also confirm `docker compose logs celery-worker` shows "Connected to redis://redis:6379/1" and task processing lines. | Single most important bug found so far — the entire exercise grading pipeline is dead in docker. Trivial fix (`.env` + a default that reuses `redis_url`), but re-tests depend on DISC-35 so we can observe the graded state. Bundle for one atomic release. |
| E2E-DISC-35 — no GET endpoint for a single submission | **high** | **Exercises Hardening PR — scheduled after the full E2E sweep closes** | Re-run **E3**: after POST, poll `GET /exercises/submissions/{id}` every 3s; receive `{status:"graded", score: n, feedback:"..."}` within 60s. Author-only access enforced (401 for other students, 200 for admin). | Backend route addition + a `useSubmissionStatus(id)` React Query hook + result rendering. Blocks DISC-34 verification (can't see the grade without it) and DISC-37 (attempt history needs a list endpoint in the same route). |
| E2E-DISC-36 — no "Show solution" affordance | low | **product decision needed** | If built: re-run **E5** — attempt 3+ times with failing code, "Reveal solution" becomes visible; click → modal shows `solution_code`; also available after a passing submission. | Engineering shouldn't guess whether reveal-on-fail is desired behavior (potential anti-cheat concern for the paid tiers). Flag for product review before touching. |
| E2E-DISC-37 — no submission-history UI | medium | **Exercises Hardening PR — scheduled after the full E2E sweep closes** | Re-run **E6**: make 2 submissions against the same exercise, page shows a history table with attempt 1 + attempt 2 rows (score, status, submitted_at, "view code" modal). Fresh page load preserves history. | Depends on DISC-35 list route. Same detail-page rewrite as DISC-32 — one PR closes both. |
| E2E-DISC-38 — no failing-submission → tutor handoff | low | **Exercises Hardening PR — scheduled after the full E2E sweep closes** (opt-in, can be dropped if out of scope) | Re-run **E8**: submit obviously-wrong code, grade returns `status=failed`, page shows "Ask the tutor" button; clicking routes to `/chat?submission_id={id}&topic=exercise_help` and chat opens with a pre-filled first message. | Lowest priority of the E-set; can slip to a follow-up PR if the hardening PR is already too big. Depends on DISC-35 (need graded output before handoff is meaningful). |
| E2E-DISC-39 — run error panel dumps raw traceback | **fixed** 2026-04-19 | Studio Hardening PR — `execution-trace.tsx` parses tracebacks + dispatches `studio.reveal_line`; `code-editor.tsx` listens + calls `editor.revealLineInCenter`. | Verified: S3 closed. `exit_code: 0` on caught exception deferred (backend `/execute` contract change). | — |
| E2E-DISC-40 — stuck banner fires with zero user activity | **fixed** 2026-04-19 | Studio Hardening PR — pointer/keyboard/wheel listeners scoped to `[data-slot="studio"]` bump `lastActivityRef`. | Verified: S9 closed. | — |
| E2E-DISC-41 — studio buttons below 44×44 tap target | **fixed** 2026-04-19 | Studio Hardening PR — `@custom-variant pointer-coarse` registered in `globals.css`; `pointer-coarse:h-11 pointer-coarse:min-w-11` applied to studio action buttons + icon-only dismiss/toggle. | Verified: S10 closed. | — |
| E2E-DISC-42 — `/agents/chat` response is Python-repr list-of-dict | **fixed** 2026-04-19 | Chat Hardening PR — orchestrator safety-net flatten via `app.agents._llm_utils.flatten_content()`. | Verified: CH1 closed. | — |
| E2E-DISC-43 — `code_review` agent wraps output as `{"raw_response": "..."}` | **fixed** 2026-04-19 | Chat Hardening PR — `extract_first_balanced_json()` + `flatten_content()` wired into `code_review.py`. | Verified: CH2 closed. Rubric-card UI deferred to next-steps. | — |
| E2E-DISC-44 — stream disconnect shows developer-facing error text | **fixed** 2026-04-19 | Chat Hardening PR — `useStream` exposes `retry()` + `error`; `ChatArea` renders "Connection lost — Retry" banner. | Verified: CH4 closed. | — |
| E2E-DISC-45 — chat history in-memory only | **fixed** 2026-04-19 | Chat Hardening PR — `localStorage["chat-conversations-v1"]` with LRU cap 20; hydration-safe restore. | Verified: CH5 closed. | — |
| E2E-DISC-46 — Enter-to-send keybinding inverted vs spec | **fixed** 2026-04-19 | Chat Hardening PR — swapped to plain Enter sends + Shift+Enter newline; Ctrl/Cmd+Enter retained as secondary. | Verified: CH8 closed. | — |
| E2E-DISC-47 — `/progress` lacks exercise%, time-watched, activity-calendar data | **fixed** 2026-04-19 | Progress Hardening PR — schema extended, KPI row rewritten, calendar unions skill + lesson-completion dates. | Verified: P1 closed. | — |
| E2E-DISC-48 — Progress page stale on refocus within `staleTime` window | **fixed** 2026-04-19 | Progress Hardening PR — per-query `staleTime: 0` + BroadcastChannel cross-tab signal. | Verified: P2 closed. | — |
| E2E-DISC-49 — Sidebar Receipts link has no unread-count badge | **fixed** 2026-04-19 | Progress Hardening PR — count-bearing pill scoped to `weekly_letter` notifications. | Verified: P3 closed. | — |
| E2E-DISC-50 — Auto-expanded weekly letter skips mark-read | **fixed** 2026-04-19 | Progress Hardening PR — mount-guarded `useEffect` fires `onLetterOpen` once when auto-expanded. | Verified: P4 closed. | — |
| E2E-DISC-51 — Fenced code blocks render verbatim in letters | **fixed** 2026-04-19 | Progress Hardening PR — `parseLetter()` block-walker emits `<pre><code>` for ```` ``` ```` fences. | Verified: P6 closed. | — |
| E2E-DISC-52 — Zombie auth state blocks admin redirect | **high** | **Auth Hardening PR — bundled with DISC-11/12/13** | Re-run **AD1**: cold-load `/admin` with an `auth-storage` manually set to `{token, isAuthenticated:true, user:null}` — guard must detect the broken state, attempt a `refreshMe()`, and either hydrate + gate correctly or redirect to `/login`. No infinite spinner. | Lives in the same auth-store surface as DISC-11 (silent refresh) and DISC-12 (cross-tab logout). Fixing in one Auth Hardening PR prevents three review passes over `auth-store.ts`. |
| E2E-DISC-53 — Admin landing lacks Enrollments + Revenue tiles | **fixed** 2026-04-19 | Admin Hardening PR — KPI grid widened to 6 tiles; Enrollments + MRR read the already-returned `total_enrollments` and `mrr_usd`. | Verified: AD2 closed 2026-04-19. | — |
| E2E-DISC-54 — Agent health missing Last-Call + Success-Rate | **fixed** 2026-04-19 | Admin Hardening PR — `/admin/agents/health` now emits `last_called_at` and `success_rate`; landing-page table + `/admin/agents` cards render both. | Verified: AD3 closed 2026-04-19. | — |
| E2E-DISC-55 — Student drilldown page + timeline endpoint both missing | **fixed** 2026-04-19 | Admin Hardening PR — `GET /admin/students/{id}/timeline` merges agent_actions + completions + submissions + last_login; new `admin/students/[id]/page.tsx` renders the feed; list rows are `<Link>`s. | Verified: AD5 closed 2026-04-19. | — |
| E2E-DISC-56 — Students search is client-only + no pagination | **fixed** 2026-04-19 | Admin Hardening PR — `GET /admin/students?q=` does server-side lower(email)/full_name match; `/admin/students` page debounces 250ms. | Verified: AD6 closed 2026-04-19. | — |
| E2E-DISC-57 — No admin agent-trigger affordance + no `actor_id` in audit log | **fixed** 2026-04-19 | Admin Hardening PR — migration 0027 adds `actor_id`/`actor_role`/`on_behalf_of`; `BaseAgent.log_action` threads them from `state.context`; `POST /admin/agents/{name}/trigger` exposes the run; drilldown page has a dropdown+Run UI; MOA keyword map routes "re-engage/inactive/churn" → `disrupt_prevention`. | Verified: AD7 + AD8 closed 2026-04-19. | — |
| E2E-DISC-58 — Admin tables not responsive at < 768px | **fixed** 2026-04-19 | Admin Hardening PR — `/admin/students` and landing-page Agent Health both ship a `md:hidden` stacked-card layout + `hidden md:block` table. | Verified manually at 375px; no horizontal overflow. | — |
| E2E-DISC-59 — Primary teal token fails WCAG AA contrast (every surface) | **fixed** 2026-04-19 | Accessibility Hardening PR — `--primary` + `--ring` + `--sidebar-primary` + `--sidebar-ring` shifted to `oklch(0.52 0.12 164)` in [globals.css](../frontend/src/app/globals.css); every primary surface now reads ~5:1 contrast vs white. | Verified: AC1, AC2 closed 2026-04-19. | — |
| E2E-DISC-60 — No skip-link + sidebar-first tab order | **fixed** 2026-04-19 | Accessibility Hardening PR — new `SkipToContent` component rendered as first focusable child of portal + admin layouts; `<main id="main-content" tabIndex={-1}>` accepts the skip-link target focus. | Verified: AC3 closed 2026-04-19. | — |
| E2E-DISC-61 — Custom mobile drawer ignores ESC | **fixed** 2026-04-19 | Accessibility Hardening PR — scoped `keydown` listener on `window` closes the drawer on `Escape` when `sidebarOpen === true`; detaches when drawer closes. | Verified: AC5 closed 2026-04-19. | — |

### "Auth Hardening PR" — explicit bundle definition

One PR will close DISC-10, DISC-11, DISC-12, DISC-13 together. **Scope:**

1. **Backend:** add `POST /api/v1/auth/refresh` — accepts the refresh token, returns a fresh access token. Reject reused refresh tokens; rotate on use.
2. **Backend:** swap slowapi's default 429 handler for one that emits `{"detail": "..."}` (FastAPI-native shape).
3. **Frontend — `api-client.ts`:** on 401, attempt `/auth/refresh` before redirecting; if refresh also 401s, append `?next={location.pathname}${location.search}` and redirect to `/login`. Single-flight the refresh (multiple concurrent 401s must only trigger one refresh).
4. **Frontend — `auth-store.ts`:** subscribe to `storage` events; when `auth-storage` is cleared from another tab, run `clearAuth()` + `router.replace('/login')`.
5. **Frontend — `login/page.tsx` + `register/page.tsx`:** read `?next=`, validate it's a same-origin absolute-path (regex `^\/[^/].*`), `router.replace(next ?? defaultLanding)`.
6. **Re-test:** A3, A6, A7, A8, A9, A10 must all flip from `failing` → `closed` with fresh 3-bullet outcomes.

**When:** After the full E2E sweep (all 13 suites) lands — currently projected for the end of this B5 cycle. We'll open the PR before CI is set up (per the "functional-correctness-first" direction).

**Who re-tests:** whoever executes the Auth Hardening PR opens re-runs A3 + A6–A10 against the docker stack, updates the tracker rows, and **only then** may flip each DISC-N row from `open` → `fixed`.

### "Onboarding Polish PR" — explicit bundle definition

One PR will close DISC-15 and DISC-16 together. **Scope:**

1. **Frontend — `goal-contract-form.tsx`:** persist `{step, motivation, deadline, successStatement}` to `sessionStorage` under key `onboarding-draft-v1` on every change; restore on mount when no `defaultValues` prop is present; clear on successful submit.
2. **Frontend — `onboarding/page.tsx`:** after `await upsert.mutateAsync(values)` resolves, call `window.dispatchEvent(new CustomEvent("onboarding.complete", { detail: { motivation, deadline_months } }))` *before* `router.push`.
3. **Re-test:** O4 and O5 must flip from `failing` → `closed`.

### "Courses Hardening PR" — explicit bundle definition

One PR will close DISC-20, DISC-21, DISC-22 together. **Scope:**

1. **Frontend — `courses/page.tsx`:** add (a) a controlled search `<input type="search">` that filters by title + description (client-side), (b) a sort `<select>` (difficulty asc, price asc, recently added), (c) windowed pagination at 12 items/page with prev/next and page numbers. Keep query state in URL search params so back/forward works.
2. **Frontend — `courses/page.tsx`:** render a dedicated search-empty-state when `filtered.length === 0 && query !== ""`: "No courses match '{query}'" + reset button.
3. **Frontend — `courses/[id]/page.tsx`:** add a `useMyEnrollments()` hook (or extend `useMyProgress` to include enrollment status); for paid courses where the user is not enrolled, render an "Enroll for ${priceDollars}" primary CTA and hide the lesson list behind enrollment. Free courses continue to auto-show lessons.
4. **Frontend — `courses/[id]/page.tsx`:** when enrolled and `completedCount > 0 && completedCount < lessons.length`, render a "Continue with {next lesson title}" primary CTA; when `completedCount === 0`, render "Start with {lessons[0].title}".
5. **Fixture:** seed ≥20 courses in the dev DB (SQL migration or seed script) so C1/C8 scroll/pagination stress-tests have enough rows.
6. **Re-test:** C1, C3, C4, C5, C6 must flip from `failing` → `closed`; C8 re-verified against the larger fixture.

### "Lessons Hardening PR" — explicit bundle definition

One PR will close DISC-23, DISC-26, DISC-27, DISC-28 together. **Scope:**

1. **Backend — `backend/app/schemas/lesson.py`:** add `video_url: str | None`, `youtube_video_id: str | None`, `content: str | None` to `LessonResponse`. Regenerate the TS client if OpenAPI is source-of-truth.
2. **Backend — `backend/app/api/v1/routes/lessons.py` (GET `/lessons/{id}`):** resolve `course.price_cents`; if >0 and no active enrollment for `current_user`, return `JSONResponse(402, {"detail":"enroll_required","course_id":str(course_id),"price_cents":course.price_cents})`.
3. **Backend — Alembic migration:** add `UniqueConstraint("student_id","lesson_id", name="uq_student_progress_student_lesson")` on `student_progress`; pre-migration step deletes duplicates keeping `MIN(id)` per (student_id, lesson_id). Switch `ProgressService.complete_lesson` to `INSERT … ON CONFLICT (student_id, lesson_id) DO UPDATE SET status, completed_at`.
4. **Frontend — `frontend/src/app/(portal)/lessons/[id]/page.tsx`:** compute `prevLesson` + `nextLesson` from `useCourseLessons(lesson.course_id)` sorted by `order`; render "Previous" (when present) and "Next" (disabled on last) at the bottom. After `completeLesson.mutate` success, if `nextLesson`, `router.push(/lessons/${nextLesson.id})`.
5. **Re-test:** L1, L5, L8, L10 must flip from `failing` → `closed`; re-verify L2 remains closed.

### "Exercises Hardening PR" — explicit bundle definition

One PR will close DISC-30, DISC-31, DISC-32, DISC-33, DISC-34, DISC-35, DISC-37 together (DISC-36 and DISC-38 remain flagged/optional). **Scope:**

1. **Backend — `backend/app/core/config.py`:** default `celery_broker_url` to `"{redis_url}/1"` derived from `redis_url` when unset; add `CELERY_BROKER_URL` + `CELERY_RESULT_BACKEND` keys to `.env.example` and document in `CLAUDE.md`. Restart `celery-worker` — logs must read "Connected to redis://redis:6379/1".
2. **Backend — `backend/app/schemas/exercise.py`:** add `description: str | None`, `starter_code: str | None`, `rubric: dict[str, Any] | None` fields to `ExerciseResponse`. Add `Field(min_length=1, max_length=20000)` to `ExerciseSubmissionCreate.code`.
3. **Backend — `backend/app/api/v1/routes/exercises.py`:** add `GET /exercises/submissions/{submission_id}` (author-or-admin gated) and `GET /exercises/{exercise_id}/submissions/mine` (current-user history). Both return full submission rows including `score`, `feedback`, `ai_feedback`, `attempt_number`, `status`.
4. **Backend — `backend/app/api/v1/routes/exercises.py`:** add `GET /exercises` (authenticated, list-all-with-status) joining `exercises` with the current user's latest submission per exercise to produce `{status, best_score}` fields; include lesson + course breadcrumb metadata.
5. **Frontend — `frontend/src/app/(portal)/exercises/page.tsx`:** delete `MOCK_EXERCISES`; add `useExercises()` hook; render real cards + zero-state + lesson breadcrumb per card.
6. **Frontend — `frontend/src/app/(portal)/exercises/[id]/page.tsx`:** render `exercise.title` heading, `exercise.description` prompt, rubric card (criterion name + weight + description), pre-fill editor with `starter_code`; add client-side code-length guard (reject empty, reject >20_000 chars) before the self-explanation modal; after submit, poll `GET /exercises/submissions/{id}` every 3s until `status != pending` (cap 60s) and render score + feedback inline; render submission-history table below the editor from `useMySubmissions(exerciseId)`.
7. **Frontend — `frontend/src/app/(portal)/exercises/[id]/page.tsx`:** wire optional side panels from the existing `/scaffold-envelope`, `/worked-example`, `/difficulty-recommendation` endpoints (Suspense-bounded, hide if `available:false`).
8. **Fixture:** `backend/scripts/seed_e2e_exercises.sql` already seeds "Write a Prompt Classifier". Keep or extend with 1 intermediate + 1 advanced exercise for list-page difficulty-filter coverage.
9. **Re-test:** E1, E2, E3, E6, E7, E9, E10 must flip from `failing` → `closed`; E4 (502 retry) becomes meaningful once DISC-34 is fixed and can be closed after a fault-injection re-run.

### "Studio Hardening PR" — explicit bundle definition

One PR will close DISC-39, DISC-40, DISC-41 together. **Scope:**

1. **Backend — `/execute` route:** on an unhandled exception, return `exit_code != 0` and route the short error summary into `stderr`; keep the full traceback in a separate `traceback` field. Update the `ExecuteResponse` Pydantic schema + TS client.
2. **Frontend — `execution-trace.tsx`:** parse `result.error` for `File "<student>", line N` — when found, highlight line N in the red error panel with a click-to-jump that calls `editor.revealLineInCenter(N)` on the Monaco instance (accessible via `studio-context`).
3. **Frontend — `stuck-banner.tsx`:** add `pointerdown`/`keydown` listeners on the studio root (via a ref passed through `studio-context` or mounted at document level with a `pointer-capture` check) that reset `lastActivityRef`; keep the 10-min "I am stuck" semantics for truly idle sessions.
4. **Frontend — studio surface:** add a Tailwind utility class on all interactive buttons: `pointer-coarse:min-h-11 pointer-coarse:min-w-11 pointer-coarse:px-3`. Alternatively hoist into a `studio-button` variant of `<Button>` and replace the raw `<button>` elements.
5. **Re-test:** S3, S9, S10 must flip from `failing` → `closed`; re-verify S4/S5/S6/S7/S8 remain closed.

### "Chat Hardening PR" — explicit bundle definition

One PR will close DISC-42, DISC-43, DISC-44, DISC-45 together (DISC-46 depends on a product decision). **Scope:**

1. **Backend — `app/services/agent_orchestrator.py` (or wherever the non-stream `/agents/chat` assembles `response`):** apply the same post-processing the stream path uses — flatten list-of-dict content blocks, skip `thinking` blocks, extract the first `text` block. Helper can live in `app/agents/_utils.py` so both paths share one code path. Matches the memory rule `feedback_llm_response_parsing.md`.
2. **Backend — `app/agents/code_review.py`:** extract the first balanced JSON object from the LLM output (same memory rule — brace-depth scan, not regex) and parse into the rubric `{score, summary, issues[]}` shape. Drop the `raw_response` wrapper path; on parse failure, set `evaluation_score: 0` and a `error: "parse_failed"` field but keep `response` as an empty rubric, not a repr string. Bump `max_tokens` to 2048+ for rubric generations per the memory rule.
3. **Frontend — new `<ChatRubricCard>` in `components/features/chat/`:** render when `agent_name === "code_review"` and parsed rubric present; score badge + summary + issue list (severity chips). Wire into the chat message component via a discriminated-union render.
4. **Frontend — `hooks/use-stream.ts:196-217`:** replace the developer error with a `<ConnectionLostBanner>` that shows "Connection lost — Retry" + a Retry button; Retry re-dispatches the last user turn against `/agents/stream`. Keep the underlying error in a dev-only `console.error`.
5. **Frontend — `app/(portal)/chat/page.tsx`:** persist `conversations` + per-conversation message arrays to `localStorage` under key `chat-conversations-v1`; hydrate on mount; LRU-cap at 20; clear on logout (subscribe to auth-store `clearAuth`). Optional follow-up: `GET /api/v1/agents/conversations` server-side list.
6. **Frontend — `app/(portal)/chat/page.tsx` (CH8 fix, pending product approval):** swap `handleKeyDown` so plain `Enter` sends (respecting `e.isComposing` and `keyCode === 229` for IME) and `Shift+Enter` inserts a newline. Leave `Ctrl/Cmd+Enter` as a secondary accelerator so users who learned the old binding don't get stuck.
7. **Re-test:** CH1 (non-stream), CH2, CH4, CH5 must flip from `failing` → `closed`; CH8 flips only after the product decision lands.

### "Progress Hardening PR" — explicit bundle definition

One PR will close DISC-47, DISC-48, DISC-49, DISC-50, DISC-51 together. **Scope:**

1. **Backend — `app/api/v1/routes/students.py` (`GET /students/me/progress`):** extend the response schema with `exercises_completed: int`, `total_exercises: int`, `watch_time_minutes: int`, `completions_by_day: list[{date: str, count: int}]` (last 90 days). Aggregate from `exercise_submissions` (status filter) + `student_progress` (`completed_at` date bucketing).
2. **Backend — `app/services/progress_service.py::complete_lesson`:** on successful completion, upsert a row into `skill_states` with `last_touched_at = now()` and the lesson's primary skill (or the lesson's `skill_id` if present on the model). Keeps `activeDaySet` sane and the activity calendar populated without requiring manual skill practice.
3. **Frontend — `app/(portal)/progress/page.tsx`:** remove the `const totalMinutes = 0` hardcode at [line 104](../frontend/src/app/(portal)/progress/page.tsx#L104) — read `progressData.watch_time_minutes` instead. Add an "Exercises" KPI tile beside "Lessons completed" / "Completion rate". Wire `completions_by_day` into the weekly-completions bar chart (last 8 weeks bucketed by `date-fns`/`Intl`).
4. **Frontend — `lib/hooks/use-progress.ts`:** override `staleTime: 5_000` for the `["progress", "mine"]` queryKey so `refetchOnWindowFocus` actually fires after a quick tab swap. Keep the global `staleTime: 60_000` for everything else.
5. **Frontend — `components/layouts/portal-sidebar.tsx`:** add a `useMyNotifications({unread_only: true})` call; render a `bg-primary text-primary-foreground text-[10px] font-semibold rounded-full px-1.5 py-0.5 min-w-[18px] text-center` badge next to the Receipts link when count > 0. `aria-label="{count} unread letters"` for a11y.
6. **Frontend — `app/(portal)/receipts/page.tsx::ReceiptCard`:** add `useEffect(() => { if (letter && !letter.is_read && expanded) { onLetterOpen(letter.id); } }, [])` (mount-only, ref-guarded). Confirms the auto-expand path also marks the letter read.
7. **Frontend — `app/(portal)/receipts/page.tsx::LetterBody`:** add a fenced-code-block case. Pre-scan lines into a mixed array of `{type: "text" | "code", content, lang?}`; render `<pre><code className="language-{lang}">…</code></pre>` for code blocks. Keep the existing `#`/`##`/`**`/`-`/`---` cases intact.
8. **Re-test:** P1, P2, P3, P4, P6 must flip from `failing` → `closed`; re-verify P5 remains closed.

### "Admin Hardening PR" — explicit bundle definition

One PR will close DISC-53, DISC-54, DISC-55, DISC-56, DISC-57 together. **Scope:**

1. **Backend — `app/api/v1/routes/admin.py`:** extend `/admin/stats` response destructuring so the frontend KPI cards can show Enrollments + MRR (already in the payload — pure frontend fix, listed under §3).
2. **Backend — `/admin/agents/health`:** add `last_called_at: datetime | None` (from `MAX(agent_actions.created_at)`) + `success_rate: float` (`COUNT(status='completed') / NULLIF(COUNT(*),0)`) to the per-agent response. Backfill `evaluation_score` writing across all 24 agents in `BaseAgent.log_action` so `avg_score` stops reading `NULL`.
3. **Backend — add `GET /admin/students/{id}/timeline`:** merged feed of `agent_actions` + `student_progress` completions + `exercise_submissions` for the student, sorted desc by ts, paginated `?limit=&before=`. Admin-only.
4. **Backend — add `GET /admin/students` server-side search/pagination:** `?q=&limit=50&offset=` with `ILIKE '%q%'` on `email || full_name`; current client-only filter deprecated.
5. **Backend — schema migration:** `ALTER TABLE agent_actions ADD COLUMN actor_id UUID NULL REFERENCES users(id), ADD COLUMN actor_role TEXT CHECK (actor_role IN ('student','admin','system')) DEFAULT 'student', ADD COLUMN on_behalf_of UUID NULL REFERENCES users(id)`. Populate from request context in `BaseAgent.log_action` — student runs default to `actor_id=student_id, actor_role='student'`, admin-triggered runs stamp the admin's JWT subject.
6. **Backend — add `POST /admin/agents/{name}/trigger`:** accepts `{student_id}`, runs the named agent via the existing runner with the admin JWT subject as `actor_id` and `on_behalf_of=student_id`; returns the standard `AgentResponse`. Whitelist of allowed agents for admin trigger (start with `disrupt_prevention`, `progress_report`, `adaptive_path`).
7. **Backend — `moa.py` keyword map:** add "re-engage / inactive / churn / outreach" patterns routing to `disrupt_prevention` so the chat surface is reachable too.
8. **Frontend — `admin/page.tsx`:** add Enrollments KPI card (`stats.total_enrollments`) + MRR card (`$${stats.mrr_usd.toLocaleString()}/mo`) matching the existing `KpiCard` pattern.
9. **Frontend — `admin/agents/page.tsx`:** add Last-Call (relative time) + Success-Rate (%) columns. Format avg-score to 2 decimals when non-null.
10. **Frontend — add `/admin/students/[id]/page.tsx`:** tabs for Lessons, Exercises, Agent activity (wired to the new timeline endpoint). Primary CTA "Actions" menu exposes "Run disrupt_prevention" / "Generate progress report" for admin-whitelisted agents.
11. **Frontend — `admin/students/page.tsx`:** wrap rows in `<Link href={`/admin/students/${id}`}>`; debounce search input 250ms and swap to server-side search/pagination from step 4.
12. **Frontend — `admin/layout.tsx` guard fix (also bundled into Auth Hardening PR as DISC-52):** treat `isAuthenticated && !user` as "try `refreshMe()` then redirect on failure" rather than swallowing into an infinite spinner. If Auth Hardening PR lands first, this section is a no-op.
13. **Re-test:** AD1–AD8 must all flip from their current state to `closed` with fresh 3-bullet outcomes.

### "Accessibility Hardening PR" — explicit bundle definition

One PR will close DISC-59, DISC-60, DISC-61 together. **Scope:**

1. **Frontend — `frontend/src/app/globals.css`:** shift the `--primary` CSS variable from the current teal (`#15a174`, ~3.15:1 vs white) to a darker green (`#0f7a56`, ~5.0:1 vs white, WCAG AA body text). Also bump `--ring` to match so focus rings (AC4) inherit the passing contrast. Keep hover/active variants in the same hue family to preserve brand recognition. Verify with a fresh axe scan on `/today`, `/courses`, `/progress`, `/admin` — all four must land at 0 serious/critical color-contrast violations.
2. **Frontend — new `components/layouts/skip-to-content.tsx`:** small component that renders `<a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50 focus:rounded-md focus:bg-primary focus:px-3 focus:py-2 focus:text-primary-foreground focus:outline-none focus:ring-2">Skip to main content</a>`. Tab-index 0, visible only on keyboard focus.
3. **Frontend — `frontend/src/components/layouts/portal-layout.tsx`:** mount `<SkipToContent />` as the **first focusable child** of the root wrapper (before sidebar, before header). Add `id="main-content"` and `tabIndex={-1}` to the `<main>` element so the skip-link's Enter keypress lands focus on the content region rather than just scrolling.
4. **Frontend — `frontend/src/components/layouts/portal-layout.tsx`:** add a `useEffect` that attaches a `keydown` listener on `window` when `sidebarOpen === true`; on `e.key === "Escape"` call `setSidebarOpen(false)` and `e.stopPropagation()`; cleanup on `false`. Guard against double-attach on strict-mode re-renders with a ref or empty dep-array pattern.
5. **Frontend — `frontend/src/app/(portal)/admin/layout.tsx`** (optional but recommended): mirror the skip-link so the admin surface also satisfies AC3. Admin KPI tables benefit even more than the portal since the sidebar has more items.
6. **Re-test:** AC1, AC2 (across `/courses`, `/progress`, `/admin`), AC3, AC5 must flip from `failing` → `closed`. AC4 re-verified — focus ring color now passes contrast. AC6 unchanged (`passing`).
7. **Regression guard:** add a CI step that runs axe-core against `/today` and `/admin` on every PR; fail the build when new serious-or-critical violations appear.

### "Today Polish PR" — explicit bundle definition

One PR will close DISC-17, DISC-18, DISC-19 together. **Scope:**

1. **Backend — `today.py` schemas:** tighten `DailyIntentionCreate.text.max_length` from 300 to 200 (matches UI counter).
2. **Backend — `today` route for intention:** compute `intention_date` from client-supplied ISO date (validated) or from the user's stored timezone; drop the raw `utcnow().date()` call.
3. **Frontend — `today-intention.tsx`:** in `handleSave`, reject `trimmed.length > MAX_LENGTH` with a visible inline error; do not call the mutation.
4. **Frontend — `today-micro-wins.tsx`:** add `const [now, setNow] = useState(Date.now()); useEffect(() => { const id = setInterval(() => setNow(Date.now()), 60_000); return () => clearInterval(id); }, [])` and thread `now` into `formatWhen`.
5. **Re-test:** T4 (IST boundary), T5 (length cap), T8 (tick) must flip to `closed`.

---

## Outcome Note Template

Copy this into a row's outcome cell when closing a test:

```
- Verified: {one sentence on what the test actually exercised end-to-end}
- Fixed / flagged: {commit link OR E2E-DISC-N reference OR "no defect"}
- Status: closed {YYYY-MM-DD}
```

Keep each bullet to one line. If a test uncovered more than one issue, file each in Discovery and reference both IDs.

---

## Done Definition (Restated from Plan §6)

A test row may be marked `closed` only when **all four** hold:

1. The spec runs green against the real docker stack (not a mock), twice in a row.
2. The 3-bullet outcome note is filled in.
3. Any defect found is either linked to a fix commit or filed in the Discovery section.
4. The test asserts the user-visible outcome, not an implementation detail (no selector-chasing).
